---
phase: 07-print-pdf-css-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
  - src/App.vue
  - src/components/results/HostCountCard.vue
  - src/components/results/VsanMaxClusterCard.vue
  - src/components/results/StretchNetworkChecklist.vue
  - src/components/results/ResultsPanel.vue
autonomous: true
requirements:
  - PRINT-01
  - PRINT-02
  - PRINT-03

must_haves:
  truths:
    - "Print output uses A4 portrait with 25mm/15mm margins"
    - "Background colors are preserved in print via print-color-adjust"
    - "Input panel and toolbar are hidden in print mode"
    - "Results panel spans full width in print mode"
    - "No result card is split across a page boundary"
  artifacts:
    - path: "src/style.css"
      provides: "@page rule and @media print body reset"
      contains: "@page"
    - path: "src/components/results/HostCountCard.vue"
      provides: "break-inside-avoid on root div"
      contains: "break-inside-avoid"
    - path: "src/components/results/VsanMaxClusterCard.vue"
      provides: "break-inside-avoid on root section"
      contains: "break-inside-avoid"
    - path: "src/components/results/StretchNetworkChecklist.vue"
      provides: "break-inside-avoid on root section"
      contains: "break-inside-avoid"
  key_links:
    - from: "src/style.css"
      to: "browser print engine"
      via: "@page rule"
      pattern: "@page"
    - from: "src/App.vue"
      to: "src/components/results/ResultsPanel.vue"
      via: "print:col-span-2 on results pane"
      pattern: "print:col-span-2"
---

<objective>
Global print CSS foundation and result card fragmentation control.

Purpose: Establish the @page geometry, print color fidelity, and card break rules that all subsequent print plans depend on. Also finalize PRINT-01 by ensuring the grid layout collapses cleanly in print mode.

Output: src/style.css with @page + @media print rules; all result cards with break-inside-avoid; App.vue grid verified for print.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-print-pdf-css-overhaul/07-RESEARCH.md

@src/style.css
@src/App.vue
@src/components/results/HostCountCard.vue
@src/components/results/VsanMaxClusterCard.vue
@src/components/results/StretchNetworkChecklist.vue
@src/components/results/ResultsPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @page rule and @media print body reset to style.css</name>
  <files>src/style.css</files>
  <read_first>src/style.css</read_first>
  <action>
Add the following CSS after the existing `@custom-variant print (@media print);` line in src/style.css:

```css
@page {
  size: A4 portrait;
  margin: 25mm 15mm 25mm 15mm;
}

@media print {
  body {
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
}
```

The @page rule sets A4 portrait with 25mm top/bottom margins (room for fixed header/footer in Plan 02) and 15mm side margins. The print-color-adjust rules preserve background colors (emerald-600 host count, chart-like colored indicators) in print output.

Do NOT move or remove the existing `@import "tailwindcss";` or `@custom-variant print (@media print);` lines. Append below them.
  </action>
  <verify>
    <automated>grep -c "@page" src/style.css && grep -c "print-color-adjust" src/style.css</automated>
  </verify>
  <acceptance_criteria>
    - src/style.css contains `@page {` on its own line
    - src/style.css contains `size: A4 portrait;`
    - src/style.css contains `margin: 25mm 15mm 25mm 15mm;`
    - src/style.css contains `-webkit-print-color-adjust: exact;`
    - src/style.css contains `print-color-adjust: exact;`
    - src/style.css still starts with `@import "tailwindcss";`
    - `npm run build` succeeds without errors
  </acceptance_criteria>
  <done>style.css has @page A4 portrait with 25mm/15mm margins and print-color-adjust: exact in @media print body rule. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add break-inside-avoid to all result cards and finalize print grid layout</name>
  <files>src/components/results/HostCountCard.vue, src/components/results/VsanMaxClusterCard.vue, src/components/results/StretchNetworkChecklist.vue, src/components/results/ResultsPanel.vue, src/App.vue</files>
  <read_first>src/components/results/HostCountCard.vue, src/components/results/VsanMaxClusterCard.vue, src/components/results/StretchNetworkChecklist.vue, src/components/results/ResultsPanel.vue, src/App.vue</read_first>
  <action>
**HostCountCard.vue** (line 18): Add `break-inside-avoid` to the root div's class attribute. The current class is:
`class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4"`
Change to:
`class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid"`

**VsanMaxClusterCard.vue** (line 25): Add `break-inside-avoid` to the root section's class attribute. The current class is:
`class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4"`
Change to:
`class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid"`

**StretchNetworkChecklist.vue** (line 14): Add `break-inside-avoid` to the root section's class attribute. The current class is:
`class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4 space-y-3"`
Change to:
`class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4 space-y-3 break-inside-avoid"`

**ResultsPanel.vue**: Add `break-inside-avoid` as a Tailwind class on the root `<div>`. Currently it is:
`class="space-y-4"`
Change to:
`class="space-y-4 print:space-y-2"`
This tightens spacing in print mode. Do NOT add break-inside-avoid to the ResultsPanel root itself (it wraps all content, not a single card).

**App.vue**: The grid layout already has `print:hidden` on the input pane and `print:col-span-2` on the results pane. Add `print:grid-cols-1` to the `<main>` element so the 2-column grid collapses to single column in print. The current class is:
`class="grid grid-cols-1 md:grid-cols-2 min-h-[calc(100vh-56px)]"`
Change to:
`class="grid grid-cols-1 md:grid-cols-2 min-h-[calc(100vh-56px)] print:grid-cols-1 print:min-h-0"`
The `print:min-h-0` prevents the viewport-height constraint from adding blank space in print.
  </action>
  <verify>
    <automated>grep -c "break-inside-avoid" src/components/results/HostCountCard.vue src/components/results/VsanMaxClusterCard.vue src/components/results/StretchNetworkChecklist.vue && grep "print:grid-cols-1" src/App.vue | wc -l && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - HostCountCard.vue root div contains class `break-inside-avoid`
    - VsanMaxClusterCard.vue root section contains class `break-inside-avoid`
    - StretchNetworkChecklist.vue root section contains class `break-inside-avoid`
    - App.vue main element contains `print:grid-cols-1`
    - App.vue main element contains `print:min-h-0`
    - ResultsPanel.vue root div contains `print:space-y-2`
    - `npm run build` succeeds
    - `npm run test` passes (no regressions)
  </acceptance_criteria>
  <done>All result cards have break-inside-avoid preventing mid-card page splits. App.vue grid collapses to single column in print. Build and tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (type-check + Vite build)
2. `npm run test` passes (142 existing tests, no regressions)
3. `grep "@page" src/style.css` returns a match
4. `grep "break-inside-avoid" src/components/results/HostCountCard.vue` returns a match
5. `grep "print:grid-cols-1" src/App.vue` returns a match
</verification>

<success_criteria>

- style.css contains @page A4 portrait rule with margins and print-color-adjust
- All 3 result card components (HostCountCard, VsanMaxClusterCard, StretchNetworkChecklist) have break-inside-avoid
- App.vue grid collapses to single column in print mode
- Build passes, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-print-pdf-css-overhaul/07-01-SUMMARY.md`
</output>
