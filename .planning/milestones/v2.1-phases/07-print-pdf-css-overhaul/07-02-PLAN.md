---
phase: 07-print-pdf-css-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/results/ResultsPanel.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - PRINT-04
  - PRINT-05

must_haves:
  truths:
    - "Every printed page shows a running header with report title and date"
    - "Every printed page shows a footer with VCF Sizer attribution"
    - "Header and footer are invisible on screen"
    - "Header and footer text is i18n-aware"
  artifacts:
    - path: "src/components/results/ResultsPanel.vue"
      provides: "Fixed-position print header and footer elements"
      contains: "print:flex"
    - path: "src/i18n/locales/en.json"
      provides: "print.header.title and print.footer.attribution keys"
      contains: "print"
  key_links:
    - from: "src/components/results/ResultsPanel.vue"
      to: "src/i18n/locales/en.json"
      via: "t('print.header.title') and t('print.footer.attribution')"
      pattern: "print\\.header\\.title"
    - from: "src/style.css"
      to: "src/components/results/ResultsPanel.vue"
      via: "@page margin-top: 25mm clears fixed header"
      pattern: "margin: 25mm"
---

<objective>
Add print-only running header and footer to ResultsPanel with i18n keys in all 4 locales.

Purpose: Every printed page must show a header with the report title and generation date, and a footer with VCF Sizer attribution (PRINT-04, PRINT-05). Uses fixed-position elements that repeat on every printed page, visible only in print mode via Tailwind `print:flex`. The 25mm @page margins from Plan 01 prevent content overlap.

Output: ResultsPanel.vue with print header/footer elements; 4 locale files with print.header.title and print.footer.attribution keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-print-pdf-css-overhaul/07-RESEARCH.md
@.planning/phases/07-print-pdf-css-overhaul/07-01-SUMMARY.md

@src/components/results/ResultsPanel.vue
@src/i18n/locales/en.json
@src/i18n/locales/fr.json
@src/i18n/locales/de.json
@src/i18n/locales/it.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print header/footer to ResultsPanel and i18n keys to all locales</name>
  <files>src/components/results/ResultsPanel.vue, src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>src/components/results/ResultsPanel.vue, src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</read_first>
  <action>
**ResultsPanel.vue** — Add script setup block and print header/footer elements:

1. Add a `<script setup lang="ts">` block (currently the component only has imports in script). Add:

```typescript
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
const reportDate = computed(() => new Date().toLocaleDateString())
```

Keep all existing component imports (HostCountCard, VsanMaxClusterCard, etc.).

1. In the template, wrap the existing content in a `<div>` and add two print-only fixed-position elements BEFORE the existing child components:

```html
<template>
  <div class="space-y-4 print:space-y-2">
    <!-- Print-only header: fixed position repeats on every page -->
    <div class="hidden print:flex fixed top-0 left-0 right-0 justify-between items-center text-xs text-gray-500 border-b border-gray-200 py-2 px-4 bg-white z-50">
      <span>{{ t('print.header.title') }}</span>
      <span>{{ reportDate }}</span>
    </div>

    <!-- Print-only footer: fixed position repeats on every page -->
    <div class="hidden print:flex fixed bottom-0 left-0 right-0 justify-center items-center text-xs text-gray-400 border-t border-gray-200 py-2 px-4 bg-white z-50">
      <span>{{ t('print.footer.attribution') }}</span>
    </div>

    <!-- Existing result components (unchanged order) -->
    <HostCountCard />
    <VsanMaxClusterCard />
    <CoresChart />
    <RamChart />
    <StorageChart />
    <StretchNetworkChecklist />
    <ExportToolbar />
  </div>
</template>
```

The `hidden print:flex` pattern makes these elements invisible on screen and visible as flexbox in print. The `fixed` position ensures they repeat on every printed page. The `z-50` ensures they render above content.

1. **i18n keys** — Add a `"print"` section to each locale file:

**en.json** — Add at the top level (alongside "app", "language", etc.):

```json
"print": {
  "header": {
    "title": "VCF 9.x Sizing Report"
  },
  "footer": {
    "attribution": "Generated by VCF Sizer"
  }
}
```

**fr.json**:

```json
"print": {
  "header": {
    "title": "Rapport de dimensionnement VCF 9.x"
  },
  "footer": {
    "attribution": "Genere par VCF Sizer"
  }
}
```

**de.json**:

```json
"print": {
  "header": {
    "title": "VCF 9.x Dimensionierungsbericht"
  },
  "footer": {
    "attribution": "Erstellt mit VCF Sizer"
  }
}
```

**it.json**:

```json
"print": {
  "header": {
    "title": "Rapporto di dimensionamento VCF 9.x"
  },
  "footer": {
    "attribution": "Generato da VCF Sizer"
  }
}
```

  </action>
  <verify>
    <automated>grep -c "print.header.title" src/components/results/ResultsPanel.vue && grep -c '"print"' src/i18n/locales/en.json src/i18n/locales/fr.json src/i18n/locales/de.json src/i18n/locales/it.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ResultsPanel.vue contains `t('print.header.title')` in template
    - ResultsPanel.vue contains `t('print.footer.attribution')` in template
    - ResultsPanel.vue contains `hidden print:flex` (twice: header and footer)
    - ResultsPanel.vue contains `fixed top-0` (header) and `fixed bottom-0` (footer)
    - ResultsPanel.vue imports `useI18n` and `computed` from vue
    - ResultsPanel.vue has `const reportDate = computed(() => new Date().toLocaleDateString())`
    - en.json contains key path `print.header.title` with value `VCF 9.x Sizing Report`
    - en.json contains key path `print.footer.attribution` with value `Generated by VCF Sizer`
    - fr.json, de.json, it.json each contain `"print"` top-level key with header.title and footer.attribution
    - `npm run build` succeeds
    - `npm run test` passes
  </acceptance_criteria>
  <done>ResultsPanel.vue has fixed-position print header (title + date) and footer (attribution) using i18n keys. All 4 locale files have print.header.title and print.footer.attribution keys. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify print layout in browser</name>
  <files>none</files>
  <action>
Human verification checkpoint. No code changes — user visually inspects the print output in a browser print preview to confirm PRINT-01 through PRINT-05 all work correctly together.
  </action>
  <what-built>Complete print layout: @page margins, hidden input panel, break-inside-avoid on cards, running header with title+date, running footer with attribution. All from Plans 01 and 02.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` (production build required for Safari compatibility)
    2. Open http://localhost:4173/vcf-sizer/ in Chrome
    3. Press Ctrl+P (or Cmd+P on Mac) to open print preview
    4. Verify: Input panel is NOT visible (only results)
    5. Verify: Page size shows A4 portrait
    6. Verify: Each page has "VCF 9.x Sizing Report" header with today's date on the right
    7. Verify: Each page has "Generated by VCF Sizer" footer centered at bottom
    8. Verify: No result card is split across page boundaries
    9. Verify: The emerald-600 host count number retains its color (not gray)
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User confirms print preview shows correct layout with header, footer, hidden inputs, and no card splits.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run test` passes
3. `grep "print.header.title" src/components/results/ResultsPanel.vue` returns a match
4. `grep "print.footer.attribution" src/components/results/ResultsPanel.vue` returns a match
5. All 4 locale files contain "print" key
6. Manual: Print preview shows header on every page
7. Manual: Print preview shows footer on every page
</verification>

<success_criteria>

- ResultsPanel.vue contains fixed-position print header and footer elements
- All 4 locale files have print.header.title and print.footer.attribution
- Header shows report title + generation date; footer shows attribution
- Elements are invisible on screen, visible in print
- Build passes, tests pass
- User approves print preview layout
</success_criteria>

<output>
After completion, create `.planning/phases/07-print-pdf-css-overhaul/07-02-SUMMARY.md`
</output>
