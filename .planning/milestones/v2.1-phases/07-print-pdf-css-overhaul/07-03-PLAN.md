---
phase: 07-print-pdf-css-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/results/charts/CoresChart.vue
  - src/components/results/charts/RamChart.vue
  - src/components/results/charts/StorageChart.vue
autonomous: true
requirements:
  - PRINT-06

must_haves:
  truths:
    - "Chart.js canvas is hidden in print mode"
    - "A plain data table with the same values replaces each chart in print"
    - "Tables use display:table in print (not display:block)"
  artifacts:
    - path: "src/components/results/charts/CoresChart.vue"
      provides: "Print fallback table for cores required vs available"
      contains: "print:table"
    - path: "src/components/results/charts/RamChart.vue"
      provides: "Print fallback table for RAM required vs available"
      contains: "print:table"
    - path: "src/components/results/charts/StorageChart.vue"
      provides: "Print fallback table for storage capacity breakdown"
      contains: "print:table"
  key_links:
    - from: "src/components/results/charts/CoresChart.vue"
      to: "useCalculationStore"
      via: "compute.totalCoresRequired, compute.availableCores in table rows"
      pattern: "compute\\.value\\.totalCoresRequired"
    - from: "src/components/results/charts/StorageChart.vue"
      to: "useCalculationStore"
      via: "storage.safeUsableCapacityTB, storage.lfsOverheadTB etc. in table rows"
      pattern: "storage\\.value\\.safeUsableCapacityTB"
---

<objective>
Add print fallback data tables to all three chart components, hiding the canvas in print.

Purpose: Chart.js canvas elements render blank in print output. Each chart needs a sibling `<table>` that is hidden on screen (`hidden`) and shown in print (`print:table`) with the same reactive data from the calculation store. This satisfies PRINT-06.

Output: CoresChart.vue, RamChart.vue, StorageChart.vue each with print:hidden on canvas wrapper and hidden print:table on a data table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-print-pdf-css-overhaul/07-RESEARCH.md

@src/components/results/charts/CoresChart.vue
@src/components/results/charts/RamChart.vue
@src/components/results/charts/StorageChart.vue

<interfaces>
<!-- Store refs already imported in each chart component -->
From CoresChart.vue / RamChart.vue:
  const { compute } = storeToRefs(calc)
  - compute.value.totalCoresRequired (number)
  - compute.value.availableCores (number)
  - compute.value.totalRamRequiredGB (number)
  - compute.value.availableRamGB (number)

From StorageChart.vue:
  const { storage } = storeToRefs(calc)

- storage.value.safeUsableCapacityTB (number)
- storage.value.lfsOverheadTB (number)
- storage.value.metadataOverheadTB (number)
- storage.value.rawCapacityTB (number)
- storage.value.usableAfterRaidTB (number)
  (RAID overhead = rawCapacityTB - usableAfterRaidTB)

Existing i18n keys used in charts:

- results.charts.cores, results.charts.ram, results.charts.storage
- results.charts.required, results.charts.available
- results.charts.storageUsable, results.charts.storageLfs
- results.charts.storageMetadata, results.charts.storageRaid
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print fallback tables to CoresChart and RamChart</name>
  <files>src/components/results/charts/CoresChart.vue, src/components/results/charts/RamChart.vue</files>
  <read_first>src/components/results/charts/CoresChart.vue, src/components/results/charts/RamChart.vue</read_first>
  <action>
**CoresChart.vue** — Modify the template section:

1. Add `break-inside-avoid` to the root div (line 68):
   `class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid"`

2. Add `print:hidden` to the chart canvas wrapper div (line 70):
   `class="h-48 relative print:hidden"`

3. Add a sibling `<table>` immediately after the canvas wrapper div, before the closing `</div>` of the root:

```html
    <!-- Print fallback: data table -->
    <table class="hidden print:table w-full text-sm border-collapse mt-2">
      <thead>
        <tr class="border-b border-gray-300">
          <th class="text-left py-1 font-medium">{{ t('results.charts.cores') }}</th>
          <th class="text-right py-1 font-medium">{{ t('results.charts.required') }}</th>
          <th class="text-right py-1 font-medium">{{ t('results.charts.available') }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td class="py-1">{{ t('results.charts.cores') }}</td>
          <td class="text-right font-mono">{{ compute.totalCoresRequired }}</td>
          <td class="text-right font-mono">{{ compute.availableCores }}</td>
        </tr>
      </tbody>
    </table>
```

**RamChart.vue** — Same pattern:

1. Add `break-inside-avoid` to root div (line 68):
   `class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid"`

2. Add `print:hidden` to canvas wrapper (line 70):
   `class="h-48 relative print:hidden"`

3. Add sibling table after canvas wrapper:

```html
    <!-- Print fallback: data table -->
    <table class="hidden print:table w-full text-sm border-collapse mt-2">
      <thead>
        <tr class="border-b border-gray-300">
          <th class="text-left py-1 font-medium">{{ t('results.charts.ram') }}</th>
          <th class="text-right py-1 font-medium">{{ t('results.charts.required') }}</th>
          <th class="text-right py-1 font-medium">{{ t('results.charts.available') }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td class="py-1">{{ t('results.charts.ram') }}</td>
          <td class="text-right font-mono">{{ compute.totalRamRequiredGB }} GB</td>
          <td class="text-right font-mono">{{ compute.availableRamGB }} GB</td>
        </tr>
      </tbody>
    </table>
```

IMPORTANT: Use `print:table` (NOT `print:block`) on `<table>` elements. `display: block` on a table breaks semantic table rendering. The `hidden` base class hides it on screen; `print:table` overrides to `display: table` in print.

No script changes needed — `compute` and `t` are already available in scope.
  </action>
  <verify>
    <automated>grep -c "print:table" src/components/results/charts/CoresChart.vue src/components/results/charts/RamChart.vue && grep -c "print:hidden" src/components/results/charts/CoresChart.vue src/components/results/charts/RamChart.vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CoresChart.vue root div contains `break-inside-avoid`
    - CoresChart.vue canvas wrapper div contains `print:hidden`
    - CoresChart.vue contains `<table class="hidden print:table`
    - CoresChart.vue table displays `compute.totalCoresRequired` and `compute.availableCores`
    - RamChart.vue root div contains `break-inside-avoid`
    - RamChart.vue canvas wrapper div contains `print:hidden`
    - RamChart.vue contains `<table class="hidden print:table`
    - RamChart.vue table displays `compute.totalRamRequiredGB` and `compute.availableRamGB`
    - Neither file uses `print:block` on a `<table>` element
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>CoresChart and RamChart have canvas hidden in print, with fallback data tables showing required vs available values. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add print fallback table to StorageChart</name>
  <files>src/components/results/charts/StorageChart.vue</files>
  <read_first>src/components/results/charts/StorageChart.vue</read_first>
  <action>
**StorageChart.vue** — Modify the template section:

1. Add `break-inside-avoid` to root div (line 85):
   `class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid"`

2. Add `print:hidden` to canvas wrapper (line 87):
   `class="h-48 relative print:hidden"`

3. Add sibling table after the canvas wrapper div. StorageChart has 4 stacked datasets (usable, LFS, metadata, RAID overhead), so the table has 4 rows:

```html
    <!-- Print fallback: data table -->
    <table class="hidden print:table w-full text-sm border-collapse mt-2">
      <thead>
        <tr class="border-b border-gray-300">
          <th class="text-left py-1 font-medium">{{ t('results.charts.storage') }}</th>
          <th class="text-right py-1 font-medium">TB</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td class="py-1">{{ t('results.charts.storageUsable') }}</td>
          <td class="text-right font-mono">{{ storage.safeUsableCapacityTB.toFixed(2) }}</td>
        </tr>
        <tr>
          <td class="py-1">{{ t('results.charts.storageLfs') }}</td>
          <td class="text-right font-mono">{{ storage.lfsOverheadTB.toFixed(2) }}</td>
        </tr>
        <tr>
          <td class="py-1">{{ t('results.charts.storageMetadata') }}</td>
          <td class="text-right font-mono">{{ storage.metadataOverheadTB.toFixed(2) }}</td>
        </tr>
        <tr>
          <td class="py-1">{{ t('results.charts.storageRaid') }}</td>
          <td class="text-right font-mono">{{ (storage.rawCapacityTB - storage.usableAfterRaidTB).toFixed(2) }}</td>
        </tr>
      </tbody>
    </table>
```

IMPORTANT: Use `print:table` not `print:block`. The RAID overhead row uses the same formula as the chart dataset: `rawCapacityTB - usableAfterRaidTB`.

No script changes needed — `storage` and `t` are already in scope.
  </action>
  <verify>
    <automated>grep -c "print:table" src/components/results/charts/StorageChart.vue && grep -c "print:hidden" src/components/results/charts/StorageChart.vue && grep "storageRaid" src/components/results/charts/StorageChart.vue | wc -l && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - StorageChart.vue root div contains `break-inside-avoid`
    - StorageChart.vue canvas wrapper div contains `print:hidden`
    - StorageChart.vue contains `<table class="hidden print:table`
    - StorageChart.vue table has 4 tbody rows: storageUsable, storageLfs, storageMetadata, storageRaid
    - StorageChart.vue RAID row uses formula `(storage.rawCapacityTB - storage.usableAfterRaidTB).toFixed(2)`
    - Does NOT use `print:block` on a `<table>` element
    - `npm run build` succeeds
    - `npm run test` passes
  </acceptance_criteria>
  <done>StorageChart has canvas hidden in print, with 4-row fallback table showing usable, LFS, metadata, and RAID overhead values. Build and tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run test` passes
3. All 3 chart files contain `print:hidden` on canvas wrapper
4. All 3 chart files contain `hidden print:table` on fallback table
5. All 3 chart files contain `break-inside-avoid` on root div
6. No chart file uses `print:block` on a `<table>` element
</verification>

<success_criteria>

- All 3 chart components (CoresChart, RamChart, StorageChart) have canvas hidden in print
- All 3 chart components have a plain data table visible only in print showing the same store values
- Tables use `print:table` (not `print:block`)
- All charts have break-inside-avoid on root element
- Build passes, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-print-pdf-css-overhaul/07-03-SUMMARY.md`
</output>
