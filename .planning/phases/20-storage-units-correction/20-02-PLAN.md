---
phase: 20-storage-units-correction
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/components/input/StorageConfigForm.vue
  - src/components/input/HostSpecsForm.vue
  - src/components/input/ManagementDomainSection.vue
  - src/components/results/DomainResultCard.vue
  - src/components/results/AggregateTotalsCard.vue
  - src/components/results/VsanMaxClusterCard.vue
  - src/components/results/charts/StorageChart.vue
  - src/composables/useMarkdownExport.ts
  - src/composables/usePptxExport.ts
  - src/composables/usePptxExport.test.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - STOR-01
  - STOR-02
  - STOR-03

must_haves:
  truths:
    - "All storage input fields display TiB unit label — no TB labels remain in UI"
    - "FC/NFS workload domain shows Total Usable Storage Pool (TiB) slider input instead of per-host storage"
    - "vSAN ESA workload domain shows Raw Storage per Host (TiB) slider"
    - "hostStorageTiB slider is hidden when storageType is fc or nfs in HostSpecsForm"
    - "All result display components show TiB not TB"
    - "Markdown and PPTX exports output TiB not TB in all storage strings"
    - "French locale uses Tio (not TiB) per French computing standards"
    - "vSAN Max profile labels show TiB/node not TB/node"
  artifacts:
    - path: "src/components/input/StorageConfigForm.vue"
      provides: "FC/NFS pool input + all TiB labels"
      contains: "externalStorageUsableTiB"
    - path: "src/components/input/HostSpecsForm.vue"
      provides: "Renamed hostStorageTiB slider with FC/NFS conditional hide"
      contains: "hostStorageTiB"
    - path: "src/i18n/locales/en.json"
      provides: "Renamed i18n keys + new externalStorageTiB key"
      contains: "storageTiB"
    - path: "src/i18n/locales/fr.json"
      provides: "French locale with Tio binary unit"
      contains: "Tio"
  key_links:
    - from: "src/components/input/StorageConfigForm.vue"
      to: "src/stores/inputStore.ts"
      via: "domainField('externalStorageUsableTiB') two-way binding"
      pattern: "domainField.*externalStorageUsableTiB"
    - from: "src/components/input/HostSpecsForm.vue"
      to: "src/stores/inputStore.ts"
      via: "domainField('hostStorageTiB') conditional on storageType"
      pattern: "hostStorageTiB"
---

<objective>
Update all Vue components, export composables, and i18n locale files to use TiB labels, renamed field bindings, and the new FC/NFS pool input. This completes the TB-to-TiB rename across the entire application surface.

Purpose: Make the TiB rename visible to end users — all UI labels, result displays, export outputs, and locale translations reflect the corrected binary unit.

Output: Complete UI/export/i18n surface with TiB labels; human-verified visual correctness via dev server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-storage-units-correction/20-RESEARCH.md
@.planning/phases/20-storage-units-correction/20-UI-SPEC.md
@.planning/phases/20-storage-units-correction/20-01-SUMMARY.md

<interfaces>
<!-- Types after Plan 01 rename — executor uses these directly -->

From src/engine/types.ts (AFTER Plan 01):

```typescript
export interface StorageInputs {
  storageType: StorageType
  hostCount: number
  hostStorageTiB: number
  externalStorageUsableTiB?: number   // FC/NFS pool — optional
  fttLevel: FttLevel
  raidType: RaidType
  dedupEnabled: boolean
  dedupRatio: number
  deploymentMode?: DeploymentMode
}

export interface StorageResult {
  rawCapacityTiB: number
  raidMultiplier: number
  usableAfterRaidTiB: number
  lfsOverheadTiB: number
  metadataOverheadTiB: number
  usableBeforeDedupTiB: number
  effectiveCapacityTiB: number
  safeUsableCapacityTiB: number
  raidScheme: string
  minHostsRequired: number
}

export interface WorkloadDomainConfig {
  // ... all existing fields ...
  hostStorageTiB: number
  externalStorageUsableTiB: number   // default: 100
  storageType: StorageType
  // ...
}

export interface ManagementDomainConfig {
  // ...
  hostStorageTiB: number
  // ...
}

export interface VsanMaxResult {
  rawCapacityTiB: number
  usableCapacityTiB: number
  raidScheme: string
  storageNodeCount: number
  computeNodeCount: number
  belowMinNodes: boolean
}

export interface AggregateTotals {
  totalRecommendedHosts: number
  mgmtHostCount: number
  totalVmCount: number
  totalRawStorageTiB: number
  totalEffectiveStorageTiB: number
  allValidationErrors: ValidationWarning[]
}

export interface StretchResult {
  // ...
  effectivePerSiteStorageTiB: number
  // ...
}
```

From src/composables/useUrlState.ts (AFTER Plan 01):

```typescript
// WorkloadDomainSchema now has:
hostStorageTiB: z.number().positive().default(3.84),
externalStorageUsableTiB: z.number().positive().default(100),
```

From src/components/shared/NumberSliderInput.vue (existing, unchanged):

```typescript
// Props: v-model (number), label (string), unit? (string), min, max, step
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update input form components — StorageConfigForm, HostSpecsForm, ManagementDomainSection</name>
  <files>
    src/components/input/StorageConfigForm.vue,
    src/components/input/HostSpecsForm.vue,
    src/components/input/ManagementDomainSection.vue
  </files>
  <action>
    **StorageConfigForm.vue** — 3 changes:

    1. Add FC/NFS pool input: After the storage type buttons row and before the vSAN ESA `<template v-if>` block, add a new block:
       ```
       <template v-if="storageType === 'fc' || storageType === 'nfs'">
         <NumberSliderInput
           v-model="externalStorageUsableTiB"
           :label="t('host.externalStorageTiB')"
           :min="1"
           :max="2000"
           :step="10"
           unit="TiB"
         />
       </template>
       ```
       In the `<script setup>`, add: `const externalStorageUsableTiB = domainField('externalStorageUsableTiB')`

    2. Rename vSAN Max profile option labels (lines 170-174):
       - `'XS — 20 TB/node'` → `'XS — 20 TiB/node'`
       - `'SM — 50 TB/node'` → `'SM — 50 TiB/node'`
       - `'MED — 100 TB/node'` → `'MED — 100 TiB/node'`
       - `'LRG — 150 TB/node'` → `'LRG — 150 TiB/node'`
       - `'XL — 200 TB/node'` → `'XL — 200 TiB/node'`

    3. Rename ALL " TB" → " TiB" in the capacity summary section (lines 196-221):
       - Line 198: `rawCapacityTB` → `rawCapacityTiB` (field access) AND `}} TB` → `}} TiB` (display text). Also rename `vsanMax.rawCapacityTB` → `vsanMax.rawCapacityTiB`.
       - Line 205: `safeUsableCapacityTB` → `safeUsableCapacityTiB` AND `}} TB` → `}} TiB`
       - Line 213: `vsanMax.usableCapacityTB` → `vsanMax.usableCapacityTiB` AND `}} TB` → `}} TiB`
       - Line 219: `rawCapacityTB` → `rawCapacityTiB` AND `}} TB` → `}} TiB`

    **HostSpecsForm.vue** — 3 changes:

    1. Rename local computed ref (line 35):
       `const hostStorageTB = domainField('hostStorageTB')` → `const hostStorageTiB = domainField('hostStorageTiB')`

    2. Rename the slider binding (lines 92-98):
       - `v-model="hostStorageTB"` → `v-model="hostStorageTiB"`
       - `:label="t('host.storageTB')"` → `:label="t('host.storageTiB')"`
       - `unit="TB"` → `unit="TiB"`

    3. Add conditional visibility — wrap the hostStorageTiB NumberSliderInput in a `v-if`:
       ```
       <NumberSliderInput
         v-if="storageType !== 'fc' && storageType !== 'nfs'"
         v-model="hostStorageTiB"
         :label="t('host.storageTiB')"
         unit="TiB"
         :min="0.96"
         :max="30.72"
         :step="0.96"
       />
       ```
       The `storageType` computed ref is already defined on line 40: `const storageType = domainField('storageType')`.

    **ManagementDomainSection.vue** — 2 changes:

    1. Rename local computed ref (line 23):
       `const hostStorageTB = mgmtField('hostStorageTB')` → `const hostStorageTiB = mgmtField('hostStorageTiB')`

    2. Rename the slider binding (lines 137-143):
       - `v-model="hostStorageTB"` → `v-model="hostStorageTiB"`
       - `:label="t('host.storageTB')"` → `:label="t('host.storageTiB')"`
       - `unit="TB"` → `unit="TiB"`
       - Keep the existing `v-if="storageType === 'vsan-esa'"` — management domain already hides storage slider for FC/NFS.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    StorageConfigForm shows FC/NFS pool slider with TiB unit when storageType is fc/nfs. vSAN Max profile labels say TiB/node. All capacity summary values display TiB. HostSpecsForm uses hostStorageTiB binding with conditional visibility hiding it for FC/NFS. ManagementDomainSection uses hostStorageTiB binding. Type-check passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update result display components + export composables — all TB to TiB</name>
  <files>
    src/components/results/DomainResultCard.vue,
    src/components/results/AggregateTotalsCard.vue,
    src/components/results/VsanMaxClusterCard.vue,
    src/components/results/charts/StorageChart.vue,
    src/composables/useMarkdownExport.ts,
    src/composables/usePptxExport.ts,
    src/composables/usePptxExport.test.ts
  </files>
  <action>
    **DomainResultCard.vue** (line ~66):
    - Rename `result.storage.safeUsableCapacityTB` → `result.storage.safeUsableCapacityTiB`
    - Rename display text `}} TB` → `}} TiB`

    **AggregateTotalsCard.vue** (lines ~50-53):
    - Line 50: `totals.totalRawStorageTB` → `totals.totalRawStorageTiB` AND `}} TB` → `}} TiB`
    - Line 53: `totals.totalEffectiveStorageTB` → `totals.totalEffectiveStorageTiB` AND `}} TB` → `}} TiB`

    **VsanMaxClusterCard.vue** (lines ~37-41):
    - Line 37: `props.result.vsanMax.rawCapacityTB` → `props.result.vsanMax.rawCapacityTiB` AND `}} TB` → `}} TiB`
    - Line 41: `vsanMax.usableCapacityTB` → `vsanMax.usableCapacityTiB` AND `}} TB` → `}} TiB`

    **StorageChart.vue** (line ~98):
    - Replace hardcoded `<th ...>TB</th>` → `<th ...>TiB</th>` in the print fallback table header.

    **useMarkdownExport.ts** — Rename ALL occurrences (per RESEARCH.md inventory):
    - Line ~72: `domain.hostStorageTB} TB` → `domain.hostStorageTiB} TiB`
    - Lines ~117-121 (5 lines): rename field accesses `*TB.toFixed(2)} TB` → `*TiB.toFixed(2)} TiB` for all StorageResult fields (rawCapacityTiB, usableAfterRaidTiB, lfsOverheadTiB, metadataOverheadTiB, safeUsableCapacityTiB, effectiveCapacityTiB, usableBeforeDedupTiB)
    - Line ~177: `effectivePerSiteStorageTB.toFixed(2)} TB` → `effectivePerSiteStorageTiB.toFixed(2)} TiB`
    - Lines ~204-205: vSAN Max `rawCapacityTB` → `rawCapacityTiB`, `usableCapacityTB` → `usableCapacityTiB` + "TB" → "TiB"
    - Lines ~221-222: aggregate `totalRawStorageTB` → `totalRawStorageTiB`, `totalEffectiveStorageTB` → `totalEffectiveStorageTiB` + "TB" → "TiB"

    **usePptxExport.ts** — Rename ALL occurrences (per RESEARCH.md inventory):
    - Line ~88: `domain.hostStorageTB} TB` → `domain.hostStorageTiB} TiB`
    - Lines ~195-196: aggregate `totalRawStorageTB` → `totalRawStorageTiB`, `totalEffectiveStorageTB` → `totalEffectiveStorageTiB` + "TB" → "TiB"
    - Line ~243: `effectivePerSiteStorageTB.toFixed(2)} TB` → `effectivePerSiteStorageTiB.toFixed(2)} TiB`
    - Lines ~270-271: vSAN Max capacity TB → TiB
    - Lines ~469-475: storage table cells with "TB" labels → "TiB"

    **usePptxExport.test.ts** — Rename assertions:
    - Lines ~312-313: `expect(rawRow!.value).toContain('TB')` → `expect(rawRow!.value).toContain('TiB')`
    - Any other assertions checking for "TB" in storage output.

    **Grep audit after changes:** Run `grep -rn '"TB"\\| TB\\|_TB\\|TB}\\|TB)' src/components/ src/composables/` to verify no remaining TB references (expect zero matches outside comments).
  </action>
  <verify>
    <automated>npm run type-check && rtk vitest run src/composables/usePptxExport.test.ts</automated>
  </verify>
  <done>
    All result display components show TiB unit labels. Both export composables output TiB in all storage-related strings. StorageChart print table header says TiB. PPTX export test passes with TiB assertions. Type-check clean. Zero "TB" storage labels remain in component templates or export template strings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all 4 i18n locale files — rename keys + add FC/NFS pool key + value changes</name>
  <files>
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json
  </files>
  <action>
    For each of the 4 locale files, make these 3 changes:

    **1. Rename `host.storageTB` key → `host.storageTiB` with updated value:**

    | File | Old key/value | New key/value |
    |------|-------------|-------------|
    | en.json | `"storageTB": "Raw Storage per Host (TB)"` | `"storageTiB": "Raw Storage per Host (TiB)"` |
    | fr.json | `"storageTB": "Stockage brut par hôte (To)"` | `"storageTiB": "Stockage brut par hôte (Tio)"` |
    | de.json | `"storageTB": "Rohspeicher pro Host (TB)"` | `"storageTiB": "Rohspeicher pro Host (TiB)"` |
    | it.json | `"storageTB": "Storage grezzo per host (TB)"` | `"storageTiB": "Storage grezzo per host (TiB)"` |

    **2. Add new `host.externalStorageTiB` key (FC/NFS pool label):**

    | File | New key/value |
    |------|-------------|
    | en.json | `"externalStorageTiB": "Total Usable Storage Pool (TiB)"` |
    | fr.json | `"externalStorageTiB": "Pool de stockage utilisable total (Tio)"` |
    | de.json | `"externalStorageTiB": "Gesamter nutzbarer Speicherpool (TiB)"` |
    | it.json | `"externalStorageTiB": "Pool di storage utilizzabile totale (TiB)"` |

    Place the new key adjacent to `storageTiB` in the `"host"` section.

    **3. Update `results.charts.storage` value (no key rename):**

    | File | Old value | New value |
    |------|-----------|-----------|
    | en.json | `"Storage (TB)"` | `"Storage (TiB)"` |
    | fr.json | `"Stockage (To)"` | `"Stockage (Tio)"` |
    | de.json | `"Speicher (TB)"` | `"Speicher (TiB)"` |
    | it.json | `"Archiviazione (TB)"` | `"Archiviazione (TiB)"` |

    **IMPORTANT:**
    - French locale uses "Tio" (tebibyte in French = tebioctet), NOT "TiB". This is per French computing standards (SI/IEC 60027-2 French translation).
    - German and Italian use "TiB" as the international abbreviation.
    - Remove the old `storageTB` key entirely from all 4 files — do not leave dead keys.

    **Post-change verification:** Run `grep -r "storageTB" src/i18n/` — must return zero matches (old key completely removed).
  </action>
  <verify>
    <automated>npm run type-check && npm run test</automated>
  </verify>
  <done>
    All 4 locale files use `storageTiB` key with TiB/Tio unit labels. New `externalStorageTiB` key added for FC/NFS pool label. `results.charts.storage` values updated to TiB/Tio. Zero remaining "storageTB" keys in i18n directory. Full test suite passes. Type-check clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — complete TiB rename visual and functional check</name>
  <action>
    Verify the complete TB-to-TiB rename across the entire application: engine types, storage calculations, store defaults, URL state schema, all Vue components (input forms, result displays, charts), export composables (Markdown, PPTX), and all 4 i18n locale files (en, fr, de, it). Verify the FC/NFS "Total Usable Storage Pool (TiB)" input replaces per-host raw storage for FC/NFS workload domains.

    Start the dev server: `npm run dev`

    1. **vSAN ESA domain (STOR-01, STOR-02):**
       - Open http://localhost:5173/vcf-sizer/
       - Complete Step 1 (select HA topology)
       - In Step 2, verify management domain storage slider shows "Raw Storage per Host (TiB)" with "TiB" unit
       - Proceed to Step 3, check default vSAN ESA workload domain
       - Verify Host Specifications section shows "Raw Storage per Host (TiB)" slider with "TiB" unit
       - Verify Storage section capacity summary shows "TiB" on raw capacity, net usable, and RAID overhead values
       - Verify results panel shows "TiB" on all storage values

    2. **FC/NFS domain (STOR-03):**
       - In the workload domain, click "FC" storage type button
       - Verify the per-host storage slider DISAPPEARS from Host Specifications
       - Verify a "Total Usable Storage Pool (TiB)" slider APPEARS in the Storage section (range 1-2000, step 10)
       - Change the pool slider value — verify the capacity summary updates accordingly
       - Click "NFS" — verify same behavior (pool slider visible, per-host hidden)
       - Click "vSAN ESA" — verify per-host slider reappears, pool slider disappears

    3. **vSAN Max labels:**
       - Click "vSAN Max" storage type
       - Verify profile dropdown options show "TiB/node" (e.g., "XS - 20 TiB/node")
       - Verify capacity summary shows TiB labels

    4. **French locale (Tio):**
       - Switch language to FR using the language selector
       - Verify storage labels show "Tio" (not "TiB") — e.g., "Stockage brut par hote (Tio)"
       - Verify results charts header shows "Stockage (Tio)"

    5. **Export check:**
       - Switch back to EN
       - Click "Export Markdown" — verify downloaded file contains "TiB" not "TB" for all storage values
       - Click "Download PPTX" — verify PPTX slides contain "TiB" not "TB" in storage table cells

    6. **URL round-trip (STOR-04):**
       - Click "Share URL" — copy the URL
       - Open a new browser tab, paste the URL
       - Verify all storage values load correctly (hostStorageTiB value preserved, FC/NFS pool value preserved)
  </action>
  <verify>
    <automated>npm run type-check && npm run test</automated>
  </verify>
  <done>Human confirms all 6 verification areas pass — TiB labels visible everywhere, FC/NFS pool input functional, French locale shows Tio, exports contain TiB, URL round-trip works.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Zod schema | externalStorageUsableTiB is a new URL-injectable field; Zod validates |
| User input → template | Storage values are numbers rendered via .toFixed() — no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | StorageConfigForm — externalStorageUsableTiB slider | mitigate | NumberSliderInput enforces min=1, max=2000 range; Zod positive() validates on URL hydration |
| T-20-04 | Spoofing | i18n locale files — Tio/TiB | accept | Label-only change; no business logic depends on display text |
</threat_model>

<verification>
1. `npm run type-check` — zero TypeScript errors
2. `npm run test` — all tests pass (279+ including new FC/NFS pool tests from Plan 01)
3. `grep -rn "storageTB" src/i18n/ src/components/ src/composables/` — zero matches (complete rename)
4. `grep -rn '" TB"' src/components/ src/composables/useMarkdownExport.ts src/composables/usePptxExport.ts` — zero matches
5. `grep -rn 'TB/node' src/components/` — zero matches (vSAN Max labels updated)
6. Human verification confirms all 6 check areas pass
</verification>

<success_criteria>

- Zero "TB" labels remain in any component template, export composable, or i18n file (excluding code comments)
- FC/NFS workload domain shows "Total Usable Storage Pool (TiB)" slider; per-host storage hidden
- vSAN ESA workload domain shows "Raw Storage per Host (TiB)" slider
- French locale shows "Tio" for the binary unit
- Markdown export contains "TiB" for all storage values
- PPTX export contains "TiB" for all storage values
- Full test suite passes: `npm run type-check && npm run test`
- Human approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/20-storage-units-correction/20-02-SUMMARY.md`
</output>
