---
phase: 05-vsan-max-storage-cluster
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/input/StorageConfigForm.vue
  - src/components/input/HostSpecsForm.vue
  - src/components/results/VsanMaxClusterCard.vue
  - src/components/results/ResultsPanel.vue
  - src/components/input/DeploymentModelSelector.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - VMAX-01
  - VMAX-02
  - VMAX-03

must_haves:
  truths:
    - "User can click 'vSAN Max' in the storage type button group and see a profile dropdown + storage nodes slider"
    - "User sees 5 profile options in the dropdown: XS through XL with TB/node labels"
    - "User sees a VsanMaxClusterCard in the results panel showing profile, node count, raw and usable capacity"
    - "User sees red warning in VsanMaxClusterCard and StorageConfigForm when storage nodes < 4"
    - "User can select network speed (10/25/100 GbE) in HostSpecsForm"
    - "User sees DEDUP_NETWORK_SPEED warning in StorageConfigForm dedup section when dedup is enabled at 10 GbE"
    - "User sees a compute cluster informational note in HostSpecsForm when vSAN Max is selected"
    - "All UI text appears in all 4 locales (en, fr, de, it)"
  artifacts:
    - path: "src/components/results/VsanMaxClusterCard.vue"
      provides: "vSAN Max storage cluster results card"
      min_lines: 40
    - path: "src/components/input/StorageConfigForm.vue"
      provides: "Extended storage config with vSAN Max section"
      contains: "vsan-max"
    - path: "src/components/input/HostSpecsForm.vue"
      provides: "Network speed button group + compute cluster note"
      contains: "networkSpeedGbE"
    - path: "src/components/results/ResultsPanel.vue"
      provides: "VsanMaxClusterCard wired after HostCountCard"
      contains: "VsanMaxClusterCard"
    - path: "src/i18n/locales/en.json"
      provides: "All Phase 5 i18n keys in English"
      contains: "vsanMax"
  key_links:
    - from: "src/components/results/VsanMaxClusterCard.vue"
      to: "src/stores/calculationStore.ts"
      via: "reads vsanMax computed"
      pattern: "calc\\.vsanMax"
    - from: "src/components/input/StorageConfigForm.vue"
      to: "src/stores/inputStore.ts"
      via: "reads/writes vsanMaxProfile, vsanMaxStorageNodes"
      pattern: "vsanMaxProfile"
    - from: "src/components/input/HostSpecsForm.vue"
      to: "src/stores/inputStore.ts"
      via: "reads/writes networkSpeedGbE"
      pattern: "networkSpeedGbE"
    - from: "src/components/results/ResultsPanel.vue"
      to: "src/components/results/VsanMaxClusterCard.vue"
      via: "imports and renders component"
      pattern: "import VsanMaxClusterCard"
---

<objective>
Build the complete UI layer for vSAN Max storage cluster selection and results display.

Purpose: Connect the engine layer (Plan 05-01) to user-facing components so architects can select vSAN Max as a storage type, configure a ReadyNode profile and storage node count, see computed results, and receive validation feedback. All UI text is externalized to 4 Swiss locale files.

Output: 1 new component (VsanMaxClusterCard.vue), 4 modified components, 4 updated locale files. Human-verified at end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vsan-max-storage-cluster/05-CONTEXT.md
@.planning/phases/05-vsan-max-storage-cluster/05-UI-SPEC.md
@.planning/phases/05-vsan-max-storage-cluster/05-01-SUMMARY.md

@src/components/input/StorageConfigForm.vue
@src/components/input/HostSpecsForm.vue
@src/components/input/DeploymentModelSelector.vue
@src/components/results/ResultsPanel.vue
@src/components/results/HostCountCard.vue
@src/components/results/StretchNetworkChecklist.vue
@src/stores/inputStore.ts
@src/stores/calculationStore.ts
@src/i18n/locales/en.json

<interfaces>
<!-- From Plan 05-01 outputs — engine layer contracts -->

From src/stores/inputStore.ts (after Plan 05-01):
```typescript
const storageType = ref<'vsan-esa' | 'fc' | 'nfs' | 'vsan-max'>('vsan-esa')
const vsanMaxProfile = ref<'xs' | 'sm' | 'med' | 'lrg' | 'xl'>('med')
const vsanMaxStorageNodes = ref(4)
const networkSpeedGbE = ref<10 | 25 | 100>(25)
```

From src/stores/calculationStore.ts (after Plan 05-01):
```typescript
const vsanMax = computed<VsanMaxResult | null>(() => ...)
// Returns null when storageType !== 'vsan-max'
// Returns VsanMaxResult when active:
interface VsanMaxResult {
  rawCapacityTB: number
  usableCapacityTB: number
  raidScheme: string
  storageNodeCount: number
  computeNodeCount: number
  belowMinNodes: boolean
}
```

From src/engine/vsanMax.ts:
```typescript
export const READYNODE_PROFILES: Record<VsanMaxProfile, { rawTbPerNode: number; label: string }>
// Profiles: xs=20, sm=50, med=100, lrg=150, xl=200
```

From src/engine/types.ts:
```typescript
export interface ValidationWarning { code: string; severity: 'error' | 'warning'; messageKey: string }
// New codes: 'VSAN_MAX_MIN_NODES' (error), 'DEDUP_NETWORK_SPEED' (warning)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StorageConfigForm + HostSpecsForm + i18n keys</name>
  <files>
    src/components/input/StorageConfigForm.vue,
    src/components/input/HostSpecsForm.vue,
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json
  </files>
  <read_first>
    src/components/input/StorageConfigForm.vue,
    src/components/input/HostSpecsForm.vue,
    src/components/input/DeploymentModelSelector.vue,
    src/stores/inputStore.ts,
    src/stores/calculationStore.ts,
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json,
    .planning/phases/05-vsan-max-storage-cluster/05-UI-SPEC.md
  </read_first>
  <action>
    **StorageConfigForm.vue changes:**

    1. Add `vsanMaxProfile` and `vsanMaxStorageNodes` to the storeToRefs destructure from inputStore:
    ```typescript
    const { storageType, fttLevel, raidType, dedupEnabled, dedupRatio, deploymentMode, vsanMaxProfile, vsanMaxStorageNodes, networkSpeedGbE } = storeToRefs(input)
    ```

    2. Add `vsanMax` to storeToRefs from calculationStore:
    ```typescript
    const { validationErrors, storage, vsanMax } = storeToRefs(calc)
    ```

    3. Add computed for validation errors:
    ```typescript
    const vsanMaxMinNodesError = computed(() =>
      validationErrors.value.find(e => e.code === 'VSAN_MAX_MIN_NODES')
    )
    const dedupNetworkSpeedError = computed(() =>
      validationErrors.value.find(e => e.code === 'DEDUP_NETWORK_SPEED')
    )
    ```

    4. Add 'vsan-max' to the storageTypes array:
    ```typescript
    const storageTypes = [
      { value: 'vsan-esa' as const, labelKey: 'storage.vsanEsa' },
      { value: 'fc' as const, labelKey: 'storage.fc' },
      { value: 'nfs' as const, labelKey: 'storage.nfs' },
      { value: 'vsan-max' as const, labelKey: 'storage.vsanMax' },
    ]
    ```

    5. Add vSAN Max conditional section AFTER the `</template>` that closes the vSAN ESA options block (after line ~115, the `</template>` for `v-if="storageType === 'vsan-esa'"`). Insert:
    ```html
    <!-- vSAN Max options (VMAX-01) -->
    <template v-if="storageType === 'vsan-max'">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <!-- Profile dropdown -->
        <div class="space-y-1">
          <label class="text-sm font-normal text-gray-700 dark:text-gray-300">
            {{ t('storage.vsanMaxProfile') }}
          </label>
          <select
            v-model="vsanMaxProfile"
            class="w-full px-2 py-1 border border-gray-300 dark:border-gray-600 rounded text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            <option value="xs">XS — 20 TB/node</option>
            <option value="sm">SM — 50 TB/node</option>
            <option value="med">MED — 100 TB/node</option>
            <option value="lrg">LRG — 150 TB/node</option>
            <option value="xl">XL — 200 TB/node</option>
          </select>
        </div>
        <!-- Storage nodes slider -->
        <NumberSliderInput
          v-model="vsanMaxStorageNodes"
          :label="t('storage.vsanMaxStorageNodes')"
          :min="4"
          :max="64"
          :step="1"
        />
      </div>
      <!-- VSAN_MAX_MIN_NODES error banner -->
      <WarningBanner
        v-if="vsanMaxMinNodesError"
        :message="t('validation.vsanMaxMinNodes')"
        severity="error"
      />
    </template>
    ```

    6. Add DEDUP_NETWORK_SPEED warning banner inside the Global Deduplication `<div class="space-y-2">` section, after the WarningBanner for dedupExclusionError and before the dedupRatio NumberSliderInput:
    ```html
    <WarningBanner
      v-if="dedupNetworkSpeedError"
      :message="t('validation.dedupNetworkSpeed')"
      severity="warning"
    />
    ```

    7. Update the storage capacity summary section at the bottom. The existing grid shows raw capacity and either vSAN details or pass-through. Add a `v-else-if` for vsan-max to show the vSAN Max storage cluster summary inline:
    ```html
    <template v-else-if="storageType === 'vsan-max' && vsanMax">
      <span>{{ t('storage.raidOverhead') }}</span>
      <span class="font-mono text-right">{{ vsanMax.raidScheme }}</span>
      <span>{{ t('storage.netUsable') }}</span>
      <span class="font-mono text-right text-green-700 dark:text-green-400 font-semibold">
        {{ vsanMax.usableCapacityTB.toFixed(2) }} TB
      </span>
    </template>
    ```
    This goes between the existing `v-if="storageType === 'vsan-esa'"` template and the `v-else` template. Update the raw capacity line to show vSAN Max raw when active (the vsanMax.rawCapacityTB from storage cluster, not the compute cluster raw from calcStorage). Wrap the first raw capacity span in a conditional:
    ```html
    <span class="font-mono text-right">
      {{ storageType === 'vsan-max' && vsanMax ? vsanMax.rawCapacityTB.toFixed(2) : storage.rawCapacityTB.toFixed(2) }} TB
    </span>
    ```

    **HostSpecsForm.vue changes:**

    1. Add `networkSpeedGbE` and `storageType` to storeToRefs destructure:
    ```typescript
    const {
      coresPerSocket, socketsPerHost, hostRamGB, hostStorageTB, hostCount,
      nvmeTieringEnabled, activeMemoryPct, networkSpeedGbE, storageType,
    } = storeToRefs(input)
    ```

    2. Insert network speed button group AFTER the hostStorageTB NumberSliderInput and BEFORE the hostCount NumberSliderInput (between lines 69-76 in current file). Use the `sm:col-span-2` class so it spans both columns:
    ```html
    <!-- Network speed selector (STOR-05, STRCH-05) -->
    <div class="space-y-1 sm:col-span-2">
      <label class="text-sm font-normal text-gray-700 dark:text-gray-300">
        {{ t('host.networkSpeed') }}
      </label>
      <div class="flex gap-2">
        <button
          v-for="speed in [10, 25, 100]"
          :key="speed"
          :class="[
            'px-3 py-1.5 text-sm rounded border font-normal transition-colors',
            networkSpeedGbE === speed
              ? 'bg-blue-600 text-white border-blue-600'
              : 'bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 border-gray-300 dark:border-gray-600 hover:border-blue-400'
          ]"
          @click="networkSpeedGbE = speed"
        >
          {{ speed }} GbE
        </button>
      </div>
    </div>
    ```

    3. Insert compute cluster informational note AFTER the hostCount NumberSliderInput, guarded by `v-if`:
    ```html
    <div
      v-if="storageType === 'vsan-max'"
      class="text-xs text-blue-700 dark:text-blue-400 italic sm:col-span-2"
    >
      {{ t('host.vsanMaxComputeNote') }}
    </div>
    ```

    **i18n keys — ALL 4 locale files in this same task/commit:**

    Add these keys to en.json (nested under appropriate sections):
    ```json
    "storage": {
      "vsanMax": "vSAN Max",
      "vsanMaxProfile": "ReadyNode Profile",
      "vsanMaxStorageNodes": "Storage Nodes"
    },
    "host": {
      "networkSpeed": "Network Speed",
      "vsanMaxComputeNote": "Host count = compute cluster size when vSAN Max is selected"
    },
    "validation": {
      "vsanMaxMinNodes": "Minimum 4 storage nodes required for vSAN Max cluster.",
      "dedupNetworkSpeed": "Global Deduplication requires 25 GbE or faster network speed (STOR-05)."
    }
    ```

    For fr.json (French Swiss):
    ```json
    "storage": {
      "vsanMax": "vSAN Max",
      "vsanMaxProfile": "Profil ReadyNode",
      "vsanMaxStorageNodes": "Noeuds de stockage"
    },
    "host": {
      "networkSpeed": "Vitesse reseau",
      "vsanMaxComputeNote": "Le nombre d'hotes = taille du cluster de calcul quand vSAN Max est selectionne"
    },
    "validation": {
      "vsanMaxMinNodes": "Minimum 4 noeuds de stockage requis pour un cluster vSAN Max.",
      "dedupNetworkSpeed": "La deduplication globale necessite un reseau 25 GbE ou plus rapide (STOR-05)."
    }
    ```

    For de.json (German Swiss):
    ```json
    "storage": {
      "vsanMax": "vSAN Max",
      "vsanMaxProfile": "ReadyNode-Profil",
      "vsanMaxStorageNodes": "Speicherknoten"
    },
    "host": {
      "networkSpeed": "Netzwerkgeschwindigkeit",
      "vsanMaxComputeNote": "Hostanzahl = Groesse des Compute-Clusters, wenn vSAN Max ausgewaehlt ist"
    },
    "validation": {
      "vsanMaxMinNodes": "Mindestens 4 Speicherknoten fuer vSAN Max-Cluster erforderlich.",
      "dedupNetworkSpeed": "Globale Deduplizierung erfordert 25 GbE oder schnellere Netzwerkgeschwindigkeit (STOR-05)."
    }
    ```

    For it.json (Italian Swiss):
    ```json
    "storage": {
      "vsanMax": "vSAN Max",
      "vsanMaxProfile": "Profilo ReadyNode",
      "vsanMaxStorageNodes": "Nodi di archiviazione"
    },
    "host": {
      "networkSpeed": "Velocita di rete",
      "vsanMaxComputeNote": "Il numero di host = dimensione del cluster di calcolo quando vSAN Max e selezionato"
    },
    "validation": {
      "vsanMaxMinNodes": "Minimo 4 nodi di archiviazione richiesti per un cluster vSAN Max.",
      "dedupNetworkSpeed": "La deduplica globale richiede una velocita di rete di 25 GbE o superiore (STOR-05)."
    }
    ```

    IMPORTANT: Merge these keys into the existing JSON structures. Do NOT overwrite existing keys. The locale files are flat or shallowly nested objects — check the existing structure and add keys at the correct nesting level.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - StorageConfigForm.vue contains `{ value: 'vsan-max'` in storageTypes array
    - StorageConfigForm.vue contains `v-if="storageType === 'vsan-max'"` template block
    - StorageConfigForm.vue contains `v-model="vsanMaxProfile"` on a select element
    - StorageConfigForm.vue contains `v-model="vsanMaxStorageNodes"` on NumberSliderInput
    - StorageConfigForm.vue contains `v-if="vsanMaxMinNodesError"` WarningBanner
    - StorageConfigForm.vue contains `v-if="dedupNetworkSpeedError"` WarningBanner
    - HostSpecsForm.vue contains `networkSpeedGbE` in storeToRefs
    - HostSpecsForm.vue contains `v-for="speed in [10, 25, 100]"` button loop
    - HostSpecsForm.vue contains `v-if="storageType === 'vsan-max'"` for compute note
    - HostSpecsForm.vue contains `t('host.vsanMaxComputeNote')`
    - en.json contains `"vsanMax"` key under storage section
    - en.json contains `"vsanMaxMinNodes"` key under validation section
    - en.json contains `"dedupNetworkSpeed"` key under validation section
    - en.json contains `"networkSpeed"` key under host section
    - fr.json contains `"vsanMaxProfile"` key
    - de.json contains `"vsanMaxProfile"` key
    - it.json contains `"vsanMaxProfile"` key
    - `npm run test` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    StorageConfigForm shows vSAN Max as a selectable storage type with profile dropdown and storage nodes slider. HostSpecsForm shows network speed selector and compute cluster note. All validation warnings wired. All 4 locale files contain all Phase 5 keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: VsanMaxClusterCard + ResultsPanel wiring + DeploymentModelSelector bandwidth cap</name>
  <files>
    src/components/results/VsanMaxClusterCard.vue,
    src/components/results/ResultsPanel.vue,
    src/components/input/DeploymentModelSelector.vue
  </files>
  <read_first>
    src/components/results/HostCountCard.vue,
    src/components/results/StretchNetworkChecklist.vue,
    src/components/results/ResultsPanel.vue,
    src/components/input/DeploymentModelSelector.vue,
    src/stores/inputStore.ts,
    src/stores/calculationStore.ts,
    src/engine/vsanMax.ts,
    .planning/phases/05-vsan-max-storage-cluster/05-UI-SPEC.md
  </read_first>
  <action>
    **Create VsanMaxClusterCard.vue:**

    Follow the HostCountCard.vue pattern exactly for card wrapper, heading, and data grid styles.

    ```vue
    <script setup lang="ts">
    import { computed } from 'vue'
    import { useI18n } from 'vue-i18n'
    import { useInputStore } from '@/stores/inputStore'
    import { useCalculationStore } from '@/stores/calculationStore'
    import { storeToRefs } from 'pinia'
    import { READYNODE_PROFILES } from '@/engine/vsanMax'
    import WarningBanner from '@/components/shared/WarningBanner.vue'

    const { t } = useI18n()
    const input = useInputStore()
    const calc = useCalculationStore()
    const { storageType } = storeToRefs(input)
    const { vsanMax } = storeToRefs(calc)

    const profileLabel = computed(() => {
      if (!vsanMax.value) return ''
      const key = input.vsanMaxProfile
      const profile = READYNODE_PROFILES[key]
      return `${key.toUpperCase()} (${profile.rawTbPerNode} TB/node)`
    })
    </script>

    <template>
      <section
        v-if="storageType === 'vsan-max' && vsanMax"
        class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4"
      >
        <h2 class="text-sm font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-3">
          {{ t('results.vsanMaxTitle') }}
        </h2>

        <!-- Profile + node count hero row -->
        <div class="flex items-center gap-6 mb-3">
          <div class="flex flex-col">
            <span
              class="text-5xl font-semibold leading-none"
              :class="vsanMax.belowMinNodes ? 'text-red-600' : 'text-emerald-600'"
            >
              {{ vsanMax.storageNodeCount }}
            </span>
            <span class="text-xs text-gray-500 dark:text-gray-400 mt-1">
              {{ t('results.vsanMaxStorageNodes') }}
            </span>
          </div>
          <div class="text-sm text-gray-600 dark:text-gray-400">
            <span class="font-normal">{{ t('results.vsanMaxProfile') }}:</span>
            {{ profileLabel }}
          </div>
        </div>

        <!-- Data grid -->
        <div class="grid grid-cols-2 gap-x-4 gap-y-1 text-xs text-gray-600 dark:text-gray-400 border-t border-gray-100 dark:border-gray-700 pt-3">
          <span>{{ t('results.vsanMaxRawCapacity') }}</span>
          <span class="font-mono text-right">{{ vsanMax.rawCapacityTB.toFixed(2) }} TB</span>

          <span>{{ t('results.vsanMaxUsableCapacity') }}</span>
          <span class="font-mono text-right text-green-700 dark:text-green-400 font-semibold">
            {{ vsanMax.usableCapacityTB.toFixed(2) }} TB
          </span>

          <span>{{ t('results.vsanMaxRaidScheme') }}</span>
          <span class="font-mono text-right">{{ vsanMax.raidScheme }}</span>
        </div>

        <!-- Min node validation error -->
        <WarningBanner
          v-if="vsanMax.belowMinNodes"
          :message="t('validation.vsanMaxMinNodes')"
          severity="error"
          class="mt-3"
        />
      </section>
    </template>
    ```

    **Update ResultsPanel.vue:**

    1. Add import for VsanMaxClusterCard:
    ```typescript
    import VsanMaxClusterCard from './VsanMaxClusterCard.vue'
    ```

    2. Insert `<VsanMaxClusterCard />` AFTER `<HostCountCard />` and BEFORE `<CoresChart />` in the template:
    ```html
    <HostCountCard />
    <VsanMaxClusterCard />
    <CoresChart />
    ```

    **Update DeploymentModelSelector.vue — stretch bandwidth cap:**

    When stretch is active and networkSpeedGbE is available, apply `Math.min(stretch.minBandwidthGbps, networkSpeedGbE)` to the displayed bandwidth.

    1. Add `networkSpeedGbE` to storeToRefs:
    ```typescript
    const { deploymentMode, preferredSiteHosts, secondarySiteHosts, managementArchitecture, networkSpeedGbE } = storeToRefs(input)
    ```

    2. Add a computed for the effective bandwidth:
    ```typescript
    const effectiveBandwidthGbps = computed(() => {
      if (!stretch.value) return 0
      return Math.min(stretch.value.minBandwidthGbps, networkSpeedGbE.value)
    })
    const bandwidthCappedByLineRate = computed(() => {
      if (!stretch.value) return false
      return stretch.value.minBandwidthGbps > networkSpeedGbE.value
    })
    ```

    3. Replace the bandwidth display line that currently shows `stretch.minBandwidthGbps.toFixed(2)`:
    ```html
    <span class="font-mono font-semibold">{{ effectiveBandwidthGbps.toFixed(2) }} Gb/s</span>
    ```

    4. Add a capped-by-line-rate indicator below the bandwidth floor indicator:
    ```html
    <span v-if="bandwidthCappedByLineRate" class="block mt-1 text-amber-600 dark:text-amber-400 text-xs italic">
      {{ t('deployment.stretchSites.bandwidthLineRateCap') }}
    </span>
    ```

    **Add remaining i18n keys to all 4 locale files (same commit):**

    en.json results section keys:
    ```json
    "results": {
      "vsanMaxTitle": "vSAN Max Storage Cluster",
      "vsanMaxStorageNodes": "Storage Nodes",
      "vsanMaxProfile": "Profile",
      "vsanMaxRawCapacity": "Raw capacity",
      "vsanMaxUsableCapacity": "Usable capacity",
      "vsanMaxRaidScheme": "RAID scheme"
    },
    "deployment": {
      "stretchSites": {
        "bandwidthLineRateCap": "Capped by network line rate"
      }
    }
    ```

    fr.json:
    ```json
    "results": {
      "vsanMaxTitle": "Cluster de stockage vSAN Max",
      "vsanMaxStorageNodes": "Noeuds de stockage",
      "vsanMaxProfile": "Profil",
      "vsanMaxRawCapacity": "Capacite brute",
      "vsanMaxUsableCapacity": "Capacite utilisable",
      "vsanMaxRaidScheme": "Schema RAID"
    },
    "deployment": {
      "stretchSites": {
        "bandwidthLineRateCap": "Limite par le debit de la ligne reseau"
      }
    }
    ```

    de.json:
    ```json
    "results": {
      "vsanMaxTitle": "vSAN Max Speichercluster",
      "vsanMaxStorageNodes": "Speicherknoten",
      "vsanMaxProfile": "Profil",
      "vsanMaxRawCapacity": "Rohkapazitaet",
      "vsanMaxUsableCapacity": "Nutzbare Kapazitaet",
      "vsanMaxRaidScheme": "RAID-Schema"
    },
    "deployment": {
      "stretchSites": {
        "bandwidthLineRateCap": "Begrenzt durch Netzwerk-Leitungsrate"
      }
    }
    ```

    it.json:
    ```json
    "results": {
      "vsanMaxTitle": "Cluster di archiviazione vSAN Max",
      "vsanMaxStorageNodes": "Nodi di archiviazione",
      "vsanMaxProfile": "Profilo",
      "vsanMaxRawCapacity": "Capacita grezza",
      "vsanMaxUsableCapacity": "Capacita utilizzabile",
      "vsanMaxRaidScheme": "Schema RAID"
    },
    "deployment": {
      "stretchSites": {
        "bandwidthLineRateCap": "Limitato dalla velocita della linea di rete"
      }
    }
    ```

    IMPORTANT: Merge these keys into the existing JSON structures. Do NOT overwrite existing keys.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/results/VsanMaxClusterCard.vue exists and contains `v-if="storageType === 'vsan-max' && vsanMax"`
    - VsanMaxClusterCard.vue contains `text-5xl font-semibold` for node count display
    - VsanMaxClusterCard.vue contains `vsanMax.belowMinNodes` conditional for red/green color
    - VsanMaxClusterCard.vue contains `import { READYNODE_PROFILES }`
    - VsanMaxClusterCard.vue contains `import WarningBanner`
    - src/components/results/ResultsPanel.vue contains `import VsanMaxClusterCard`
    - ResultsPanel.vue template has VsanMaxClusterCard after HostCountCard and before CoresChart
    - src/components/input/DeploymentModelSelector.vue contains `effectiveBandwidthGbps`
    - DeploymentModelSelector.vue contains `bandwidthCappedByLineRate`
    - DeploymentModelSelector.vue contains `networkSpeedGbE` in storeToRefs
    - en.json contains `"vsanMaxTitle"` key
    - fr.json contains `"vsanMaxTitle"` key
    - de.json contains `"vsanMaxTitle"` key
    - it.json contains `"vsanMaxTitle"` key
    - en.json contains `"bandwidthLineRateCap"` key
    - `npm run test` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    VsanMaxClusterCard renders storage cluster results with profile, node count, capacity, and warning. ResultsPanel wires the card after HostCountCard. DeploymentModelSelector caps stretch bandwidth by network line rate. All locale files updated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification</name>
  <files>none (verification-only checkpoint)</files>
  <action>
    Human verifies the complete vSAN Max feature visually and functionally in the browser. No code changes in this task. Run `npm run dev` and follow the verification steps below.
  </action>
  <what-built>
    Complete vSAN Max UI: storage type selection with profile dropdown and storage nodes slider, network speed button group, VsanMaxClusterCard in results panel, validation warnings, stretch bandwidth line-rate cap, and all 4 locale translations.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a browser
    2. In StorageConfigForm, click "vSAN Max" button — confirm profile dropdown and storage nodes slider appear
    3. Select "XL" profile — confirm dropdown shows "XL — 200 TB/node"
    4. Set storage nodes to 8 — confirm VsanMaxClusterCard shows raw=1600 TB, usable capacity, raidScheme
    5. Set storage nodes to 4 — confirm raidScheme shows "2+1 (FTT=1 RAID-5)"
    6. Set storage nodes slider minimum to 4 — confirm NO red warning appears
    7. In HostSpecsForm, confirm 3 network speed buttons (10/25/100 GbE) appear, 25 GbE is default active
    8. Click "10 GbE" — in StorageConfigForm select "vSAN ESA" — enable Global Dedup — confirm yellow DEDUP_NETWORK_SPEED warning appears
    9. Click "25 GbE" — confirm warning disappears
    10. Switch back to "vSAN Max" — confirm informational note "Host count = compute cluster size..." appears below host count slider
    11. Switch deployment to Stretch, set speeds — confirm bandwidth recommendation does not exceed networkSpeedGbE
    12. Switch language to FR, DE, IT — confirm all new labels translated (no missing keys)
    13. Copy share URL, open in new tab — confirm vSAN Max config (profile, storage nodes, network speed) is restored
  </how-to-verify>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -5</automated>
  </verify>
  <done>Human approved the vSAN Max feature visually and functionally across all 4 locales.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` passes all tests (0 failures)
- `npx tsc --noEmit` passes with no type errors
- Dev server renders all new components without console errors
- All 4 locales display Phase 5 keys correctly
- VsanMaxClusterCard appears only when vSAN Max is selected
- Share URL round-trips vSAN Max configuration
</verification>

<success_criteria>
1. User can select "vSAN Max" as storage type and see profile dropdown (5 options) + storage nodes slider (min 4)
2. VsanMaxClusterCard renders in results panel showing profile, node count, raw capacity, usable capacity, RAID scheme
3. Red warning appears when storage nodes < 4 (in both input and output sections)
4. Network speed button group (10/25/100 GbE) visible in HostSpecsForm with 25 GbE default
5. DEDUP_NETWORK_SPEED warning appears when dedup enabled at 10 GbE
6. Stretch bandwidth capped by network line rate with visible indicator
7. Compute cluster informational note visible when vSAN Max is active
8. All i18n keys present in all 4 locales
9. Share URL preserves vSAN Max configuration across page loads
10. All tests pass, TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-vsan-max-storage-cluster/05-02-SUMMARY.md`
</output>
