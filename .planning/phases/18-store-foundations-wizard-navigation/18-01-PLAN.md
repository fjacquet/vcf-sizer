---
phase: 18-store-foundations-wizard-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/uiStore.ts
  - src/stores/uiStore.test.ts
  - src/components/shared/WizardStepper.vue
autonomous: true
requirements:
  - WIZARD-01

must_haves:
  truths:
    - "uiStore exposes isLandingVisible ref initialized to true"
    - "uiStore exposes dismissLanding() action that sets isLandingVisible to false"
    - "uiStore exposes chartImages ref initialized to empty object"
    - "uiStore exposes registerChartImage() action that stores dataUrl keyed by domainId and chartType"
    - "ChartType union type is exported from uiStore.ts"
    - "Clicking a completed (green) step badge in WizardStepper calls setWizardStep(N) for that step"
    - "Clicking an active (blue) or future (grey) step badge in WizardStepper does nothing"
    - "Completed step badges show cursor-pointer and hover:ring-2 hover:ring-green-400"
  artifacts:
    - path: "src/stores/uiStore.ts"
      provides: "isLandingVisible, dismissLanding, chartImages, registerChartImage, ChartType export"
      exports: ["isLandingVisible", "dismissLanding", "chartImages", "registerChartImage"]
    - path: "src/stores/uiStore.test.ts"
      provides: "Tests for landing view state and chartImages registry"
      contains: "describe('uiStore -- landing view"
    - path: "src/components/shared/WizardStepper.vue"
      provides: "Clickable completed step badges with backward-only guard"
      contains: "cursor-pointer"
  key_links:
    - from: "src/components/shared/WizardStepper.vue"
      to: "src/stores/uiStore.ts"
      via: "@click handler calling ui.setWizardStep(s.step)"
      pattern: "ui\\.setWizardStep\\(s\\.step\\)"
---

<objective>
Add Phase 18 store foundations to uiStore (isLandingVisible, dismissLanding, chartImages, registerChartImage) with TDD tests, and implement WIZARD-01 clickable completed step badges in WizardStepper.

Purpose: Establish the store API surface consumed by Plan 02 (LandingView) and downstream phases (21, 22), and deliver the click-back navigation feature (WIZARD-01).
Output: Updated uiStore.ts with 4 new exports + passing tests, WizardStepper.vue with click-back on completed steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-store-foundations-wizard-navigation/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/stores/uiStore.ts (current return shape):
```typescript
return { locale, setLocale, currentWizardStep, setWizardStep, topologyConfirmed, confirmTopology }
```

From src/stores/uiStore.test.ts (existing test pattern):
```typescript
import { createPinia, setActivePinia } from 'pinia'
import { useUiStore } from './uiStore'
// beforeEach: setActivePinia(createPinia())
// Tests access store.propertyName directly
```

From src/components/shared/WizardStepper.vue (step pill class structure):
```html
<div :class="['flex items-center gap-1.5 px-3 py-1.5 text-sm rounded-full border font-medium',
  ui.currentWizardStep === s.step
    ? 'bg-blue-600 text-white border-blue-600'
    : ui.currentWizardStep > s.step
      ? 'bg-green-100 text-green-700 border-green-400 dark:bg-green-900 dark:text-green-300 dark:border-green-700'
      : 'bg-white dark:bg-gray-800 text-gray-400 border-gray-200 dark:border-gray-700']"
>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add uiStore landing view + chartImages state with TDD tests</name>
  <files>src/stores/uiStore.ts, src/stores/uiStore.test.ts</files>
  <read_first>
    - src/stores/uiStore.ts (current store shape — 47 lines)
    - src/stores/uiStore.test.ts (existing test patterns — 88 lines)
    - src/composables/useUrlState.ts (confirm isLandingVisible and chartImages are NOT in InputStateSchema)
  </read_first>
  <behavior>
    - Test: isLandingVisible initializes to true
    - Test: dismissLanding() sets isLandingVisible to false
    - Test: dismissLanding() is idempotent (calling twice still false)
    - Test: chartImages initializes to empty object {}
    - Test: registerChartImage('domain-1', 'cores', 'data:image/png;base64,abc') stores value at chartImages['domain-1']['cores']
    - Test: registerChartImage() for a second domain creates a separate nested record
    - Test: registerChartImage() for same domainId + same chartType overwrites previous value
    - Test: isLandingVisible is orthogonal to topologyConfirmed (confirmTopology does not change isLandingVisible)
  </behavior>
  <action>
    RED phase: Add two new `describe` blocks to `src/stores/uiStore.test.ts`:

    1. `describe('uiStore -- landing view (WIZARD-02)')` with tests for:
       - `isLandingVisible` initializes to `true`
       - `dismissLanding()` sets `isLandingVisible` to `false`
       - `dismissLanding()` is idempotent
       - `isLandingVisible` is orthogonal to `topologyConfirmed`

    2. `describe('uiStore -- chartImages registry')` with tests for:
       - `chartImages` initializes to `{}`
       - `registerChartImage('domain-1', 'cores', 'data:image/png;base64,abc')` stores at `chartImages['domain-1']['cores']`
       - Second domain creates separate record
       - Same domainId + chartType overwrites

    Run tests — all 8 new tests MUST fail (RED).

    GREEN phase: Add to `src/stores/uiStore.ts` inside the `defineStore` callback:

    1. Export `type ChartType = 'cores' | 'ram' | 'storage'` BEFORE the `defineStore` call so it is importable by downstream phases.
    2. Add `const isLandingVisible = ref<boolean>(true)`
    3. Add `function dismissLanding(): void { isLandingVisible.value = false }`
    4. Add `const chartImages = ref<Record<string, Record<ChartType, string>>>({})`
    5. Add:
       ```typescript
       function registerChartImage(domainId: string, chartType: ChartType, dataUrl: string): void {
         if (!chartImages.value[domainId]) {
           chartImages.value[domainId] = {} as Record<ChartType, string>
         }
         chartImages.value[domainId][chartType] = dataUrl
       }
       ```
    6. Extend the return statement to include all 4 new exports:
       ```typescript
       return { locale, setLocale, currentWizardStep, setWizardStep, topologyConfirmed, confirmTopology, isLandingVisible, dismissLanding, chartImages, registerChartImage }
       ```

    Run tests — all 8 new tests MUST pass (GREEN). All existing tests (271+) MUST still pass.

    CONSTRAINT: Do NOT add `isLandingVisible` or `chartImages` to `InputStateSchema` in `useUrlState.ts`. These are ephemeral UI state (WIZARD-07).
  </action>
  <verify>
    <automated>rtk vitest run src/stores/uiStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "isLandingVisible" src/stores/uiStore.ts returns at least 3 (declaration + return + function reference)
    - grep -c "dismissLanding" src/stores/uiStore.ts returns at least 2 (function definition + return)
    - grep -c "chartImages" src/stores/uiStore.ts returns at least 3 (declaration + return + function reference)
    - grep -c "registerChartImage" src/stores/uiStore.ts returns at least 2 (function definition + return)
    - grep "export type ChartType" src/stores/uiStore.ts returns a match
    - grep -c "landing view" src/stores/uiStore.test.ts returns at least 1 (describe block)
    - grep -c "chartImages" src/stores/uiStore.test.ts returns at least 4 (tests)
    - grep "isLandingVisible" src/composables/useUrlState.ts returns NO matches (not in URL schema)
    - rtk vitest run src/stores/uiStore.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>uiStore exposes isLandingVisible (init: true), dismissLanding(), chartImages (init: {}), registerChartImage(), and ChartType export. All 8 new tests pass. All 271+ existing tests still pass. Neither isLandingVisible nor chartImages appear in InputStateSchema.</done>
</task>

<task type="auto">
  <name>Task 2: Add click-back navigation to completed WizardStepper step badges (WIZARD-01)</name>
  <files>src/components/shared/WizardStepper.vue</files>
  <read_first>
    - src/components/shared/WizardStepper.vue (current template — 99 lines)
    - src/stores/uiStore.ts (confirm setWizardStep exists)
    - .planning/phases/18-store-foundations-wizard-navigation/18-UI-SPEC.md (interaction contract for WIZARD-01)
  </read_first>
  <action>
    Modify `src/components/shared/WizardStepper.vue` template — the step indicator `div` inside the `v-for` loop (line ~62-69).

    1. Add `@click` handler to the step pill `div`:
       ```
       @click="ui.currentWizardStep > s.step && ui.setWizardStep(s.step)"
       ```
       This guard ensures only completed steps (green, `currentWizardStep > s.step`) trigger navigation. Active (blue) and future (grey) steps do nothing.

    2. Add `role="button"` attribute to the step pill `div` for accessibility (per UI-SPEC.md accessibility contract — bare `div @click` is not keyboard-accessible):
       ```
       :role="ui.currentWizardStep > s.step ? 'button' : undefined"
       ```

    3. Add `tabindex` for keyboard accessibility on completed steps only:
       ```
       :tabindex="ui.currentWizardStep > s.step ? 0 : undefined"
       ```

    4. Add keyboard handler for Enter/Space activation on completed steps:
       ```
       @keydown.enter.space.prevent="ui.currentWizardStep > s.step && ui.setWizardStep(s.step)"
       ```

    5. Update the completed step class (the second ternary branch) to append `cursor-pointer hover:ring-2 hover:ring-green-400 transition-colors`:
       ```
       'bg-green-100 text-green-700 border-green-400 dark:bg-green-900 dark:text-green-300 dark:border-green-700 cursor-pointer hover:ring-2 hover:ring-green-400 transition-colors'
       ```

    The resulting step pill div should be:
    ```html
    <div
      :class="['flex items-center gap-1.5 px-3 py-1.5 text-sm rounded-full border font-medium',
        ui.currentWizardStep === s.step
          ? 'bg-blue-600 text-white border-blue-600'
          : ui.currentWizardStep > s.step
            ? 'bg-green-100 text-green-700 border-green-400 dark:bg-green-900 dark:text-green-300 dark:border-green-700 cursor-pointer hover:ring-2 hover:ring-green-400 transition-colors'
            : 'bg-white dark:bg-gray-800 text-gray-400 border-gray-200 dark:border-gray-700']"
      :role="ui.currentWizardStep > s.step ? 'button' : undefined"
      :tabindex="ui.currentWizardStep > s.step ? 0 : undefined"
      @click="ui.currentWizardStep > s.step && ui.setWizardStep(s.step)"
      @keydown.enter.space.prevent="ui.currentWizardStep > s.step && ui.setWizardStep(s.step)"
    >
    ```

    Do NOT add click handlers to active (blue) or future (grey) step pills. Future steps must have no pointer cursor — they remain neutral grey with no interaction affordance.
  </action>
  <verify>
    <automated>rtk vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep "cursor-pointer" src/components/shared/WizardStepper.vue returns a match
    - grep "hover:ring-2" src/components/shared/WizardStepper.vue returns a match
    - grep "hover:ring-green-400" src/components/shared/WizardStepper.vue returns a match
    - grep "ui.setWizardStep(s.step)" src/components/shared/WizardStepper.vue returns a match
    - grep "currentWizardStep > s.step" src/components/shared/WizardStepper.vue returns at least 2 matches (class condition + click guard)
    - grep 'role=' src/components/shared/WizardStepper.vue returns a match (accessibility)
    - grep 'tabindex' src/components/shared/WizardStepper.vue returns a match (keyboard accessibility)
    - The string "cursor-pointer" does NOT appear in the active step class (bg-blue-600 branch) or future step class (bg-white branch)
    - rtk vitest run exits 0 (all 271+ tests pass, no regressions)
    - rtk npm run type-check exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Completed (green) wizard step badges are clickable with cursor-pointer + hover ring. Clicking them calls setWizardStep(N) to navigate back. Active and future steps have no click handler. Keyboard accessible via role="button" + tabindex + keydown handler. All tests pass. Type check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 18 Plan 01 adds store state and a template click handler — no user input crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | uiStore.setWizardStep | accept | setWizardStep already constrains type to `1 \| 2 \| 3`; click guard prevents forward jumps. No PII or authorization at stake — wizard step is ephemeral UI state. |
| T-18-02 | Information Disclosure | chartImages registry | accept | chartImages stores base64 PNG data URLs of chart renderings (Phase 21). These contain no PII — only chart visualizations of sizing data. No server interaction. |
</threat_model>

<verification>
1. `rtk vitest run src/stores/uiStore.test.ts` — all new landing view + chartImages tests pass
2. `rtk vitest run` — full suite green (271+ tests, no regressions)
3. `rtk npm run type-check` — zero TypeScript errors
4. Manual: open dev server, complete step 1+2, click step 1 badge — navigates back to step 1 content
5. Manual: click step 3 badge while on step 1 — nothing happens (forward jump blocked)
</verification>

<success_criteria>
- uiStore.ts exports: isLandingVisible, dismissLanding, chartImages, registerChartImage, ChartType
- 8 new unit tests pass covering landing view state and chartImages registry
- WizardStepper completed step pills are clickable with backward-only guard
- All existing 271+ tests remain green
- Type check passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-store-foundations-wizard-navigation/18-01-SUMMARY.md`
</output>
