---
phase: 18-store-foundations-wizard-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/components/shared/LandingView.vue
  - src/App.vue
  - src/main.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - WIZARD-02

must_haves:
  truths:
    - "User sees a landing/intro view on first load that explains the 3-step VCF sizing flow"
    - "Landing view displays a 'Start Sizing' CTA button that dismisses the landing and shows the wizard"
    - "User arriving via a shared URL (?s= parameter) bypasses the landing view entirely"
    - "The sticky header with LanguageSwitcher remains visible while landing view is shown"
    - "Landing view content uses i18n keys for all visible text — no raw English strings"
    - "All 4 locale files (en, fr, de, it) contain the landing.* key namespace"
  artifacts:
    - path: "src/components/shared/LandingView.vue"
      provides: "Full-screen landing intro component with 3-step summary and CTA"
      min_lines: 40
    - path: "src/App.vue"
      provides: "v-if/v-else conditional rendering of LandingView vs wizard layout"
      contains: "LandingView"
    - path: "src/main.ts"
      provides: "dismissLanding() call on URL hydration path"
      contains: "dismissLanding"
    - path: "src/i18n/locales/en.json"
      provides: "English landing.* i18n keys"
      contains: "landing"
  key_links:
    - from: "src/components/shared/LandingView.vue"
      to: "src/stores/uiStore.ts"
      via: "ui.dismissLanding() on CTA click"
      pattern: "dismissLanding"
    - from: "src/App.vue"
      to: "src/stores/uiStore.ts"
      via: "v-if on ui.isLandingVisible"
      pattern: "ui\\.isLandingVisible"
    - from: "src/main.ts"
      to: "src/stores/uiStore.ts"
      via: "uiStore.dismissLanding() in URL hydration block"
      pattern: "dismissLanding"
---

<objective>
Create the LandingView component (WIZARD-02) with i18n support across all 4 locales, integrate it into App.vue with v-if/v-else conditional rendering, and wire main.ts to bypass landing on URL-hydrated sessions.

Purpose: New users see an intro screen explaining the 3-step sizing workflow before entering the wizard. Users arriving via shared URLs skip directly to their hydrated configuration.
Output: LandingView.vue component, updated App.vue, updated main.ts, landing.* i18n keys in all 4 locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-store-foundations-wizard-navigation/18-RESEARCH.md
@.planning/phases/18-store-foundations-wizard-navigation/18-UI-SPEC.md
@.planning/phases/18-store-foundations-wizard-navigation/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output. -->

From src/stores/uiStore.ts (after Plan 01):
```typescript
export type ChartType = 'cores' | 'ram' | 'storage'

// Inside defineStore return:
return {
  locale, setLocale,
  currentWizardStep, setWizardStep,
  topologyConfirmed, confirmTopology,
  isLandingVisible, dismissLanding,
  chartImages, registerChartImage,
}
```

From src/App.vue (current structure):
```html
<header class="... sticky top-0 z-10 print:hidden">
  <h1>{{ t('app.title') }}</h1>
  <LanguageSwitcher />
</header>
<main class="grid grid-cols-1 md:grid-cols-2 min-h-[calc(100vh-56px)]">
  <!-- LEFT PANE: wizard steps -->
  <!-- RIGHT PANE: results -->
</main>
```

From src/main.ts (URL hydration block):
```typescript
if (window.location.search.includes('s=')) {
  const uiStore = useUiStore()
  uiStore.confirmTopology()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add landing.* i18n keys to all 4 locale files and create LandingView.vue component</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json, src/components/shared/LandingView.vue</files>
  <read_first>
    - src/i18n/locales/en.json (current key structure — find where to add landing namespace)
    - src/i18n/locales/fr.json (translation pattern — verify top-level namespace structure)
    - src/i18n/locales/de.json (translation pattern)
    - src/i18n/locales/it.json (translation pattern)
    - src/stores/uiStore.ts (confirm isLandingVisible and dismissLanding exist from Plan 01)
    - .planning/phases/18-store-foundations-wizard-navigation/18-UI-SPEC.md (copywriting contract + layout spec)
  </read_first>
  <action>
    **Step 1: Add landing.* i18n keys to en.json**

    Add a new top-level `"landing"` object to `src/i18n/locales/en.json` (at the same level as `"app"`, `"deployment"`, etc.):

    ```json
    "landing": {
      "title": "Right-size your VCF 9.x deployment before hardware is ordered",
      "subtitle": "Answer three guided questions. Get exact host counts, RAM, and storage requirements.",
      "step1": {
        "title": "Topology",
        "desc": "Choose Simple, HA, or Stretch Cluster for your deployment model."
      },
      "step2": {
        "title": "Management",
        "desc": "Configure the management domain architecture and host specifications."
      },
      "step3": {
        "title": "Workloads",
        "desc": "Define workload domain profiles. Results update in real time."
      },
      "cta": "Start Sizing"
    }
    ```

    **Step 2: Add landing.* i18n keys to fr.json**

    ```json
    "landing": {
      "title": "Dimensionnez votre deploiement VCF 9.x avant de commander le materiel",
      "subtitle": "Repondez a trois questions guidees. Obtenez le nombre exact d'hotes, la RAM et les besoins en stockage.",
      "step1": {
        "title": "Topologie",
        "desc": "Choisissez Simple, HA ou Stretch Cluster pour votre modele de deploiement."
      },
      "step2": {
        "title": "Gestion",
        "desc": "Configurez l'architecture du domaine de gestion et les specifications des hotes."
      },
      "step3": {
        "title": "Charges de travail",
        "desc": "Definissez les profils de domaine de charge de travail. Les resultats se mettent a jour en temps reel."
      },
      "cta": "Commencer le dimensionnement"
    }
    ```

    **Step 3: Add landing.* i18n keys to de.json**

    ```json
    "landing": {
      "title": "Dimensionieren Sie Ihre VCF 9.x-Bereitstellung vor der Hardwarebestellung",
      "subtitle": "Beantworten Sie drei gefuhrte Fragen. Erhalten Sie die genaue Anzahl Hosts, RAM und Speicheranforderungen.",
      "step1": {
        "title": "Topologie",
        "desc": "Wahlen Sie Simple, HA oder Stretch Cluster fur Ihr Bereitstellungsmodell."
      },
      "step2": {
        "title": "Verwaltung",
        "desc": "Konfigurieren Sie die Architektur der Verwaltungsdomane und die Host-Spezifikationen."
      },
      "step3": {
        "title": "Workloads",
        "desc": "Definieren Sie Workload-Domanen-Profile. Die Ergebnisse aktualisieren sich in Echtzeit."
      },
      "cta": "Dimensionierung starten"
    }
    ```

    **Step 4: Add landing.* i18n keys to it.json**

    ```json
    "landing": {
      "title": "Dimensiona il tuo deployment VCF 9.x prima di ordinare l'hardware",
      "subtitle": "Rispondi a tre domande guidate. Ottieni il numero esatto di host, RAM e requisiti di storage.",
      "step1": {
        "title": "Topologia",
        "desc": "Scegli Simple, HA o Stretch Cluster per il tuo modello di deployment."
      },
      "step2": {
        "title": "Gestione",
        "desc": "Configura l'architettura del dominio di gestione e le specifiche degli host."
      },
      "step3": {
        "title": "Carichi di lavoro",
        "desc": "Definisci i profili dei domini di carico di lavoro. I risultati si aggiornano in tempo reale."
      },
      "cta": "Inizia il dimensionamento"
    }
    ```

    **Step 5: Create `src/components/shared/LandingView.vue`**

    Create a new SFC with this exact structure (per UI-SPEC.md layout contract):

    ```vue
    <script setup lang="ts">
    import { useUiStore } from '@/stores/uiStore'
    import { useI18n } from 'vue-i18n'

    const ui = useUiStore()
    const { t } = useI18n()

    const steps = [
      { num: 1, titleKey: 'landing.step1.title', descKey: 'landing.step1.desc' },
      { num: 2, titleKey: 'landing.step2.title', descKey: 'landing.step2.desc' },
      { num: 3, titleKey: 'landing.step3.title', descKey: 'landing.step3.desc' },
    ]
    </script>

    <template>
      <div class="min-h-[calc(100vh-56px)] flex flex-col items-center justify-center bg-gray-50 dark:bg-gray-950 px-4 py-12 print:hidden">
        <h2 class="text-2xl md:text-3xl font-semibold text-gray-900 dark:text-gray-100 text-center max-w-2xl">
          {{ t('landing.title') }}
        </h2>
        <p class="mt-4 text-sm text-gray-600 dark:text-gray-400 text-center max-w-lg">
          {{ t('landing.subtitle') }}
        </p>

        <div class="mt-8 grid grid-cols-1 md:grid-cols-3 gap-4 max-w-3xl w-full">
          <div
            v-for="step in steps"
            :key="step.num"
            class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-4 flex flex-col items-center text-center"
          >
            <div
              class="w-8 h-8 rounded-full bg-blue-600 text-white flex items-center justify-center text-sm font-bold"
              aria-hidden="true"
            >
              {{ step.num }}
            </div>
            <h3 class="mt-3 text-base font-semibold text-gray-900 dark:text-gray-100">
              {{ t(step.titleKey) }}
            </h3>
            <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">
              {{ t(step.descKey) }}
            </p>
          </div>
        </div>

        <button
          class="mt-8 bg-blue-600 text-white hover:bg-blue-700 px-6 py-3 text-base font-semibold rounded-md transition-colors"
          @click="ui.dismissLanding()"
        >
          {{ t('landing.cta') }}
        </button>
      </div>
    </template>
    ```

    Key points:
    - `min-h-[calc(100vh-56px)]` matches the main content area height (header is ~56px)
    - `print:hidden` prevents landing from appearing in print output
    - Step number badges have `aria-hidden="true"` (decorative — step titles carry semantic meaning)
    - CTA is a `<button>` element (not div/a) for accessibility
    - CTA calls `ui.dismissLanding()` directly — no emit needed
    - All visible text uses `t()` calls — zero raw English strings
    - Dark mode variants on all color classes
  </action>
  <verify>
    <automated>rtk npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/shared/LandingView.vue exists and is at least 40 lines
    - grep "landing.title" src/i18n/locales/en.json returns a match
    - grep "landing.cta" src/i18n/locales/en.json returns a match
    - grep "landing.step1.title" src/i18n/locales/en.json returns a match
    - grep "landing" src/i18n/locales/fr.json returns at least 1 match
    - grep "landing" src/i18n/locales/de.json returns at least 1 match
    - grep "landing" src/i18n/locales/it.json returns at least 1 match
    - grep "dismissLanding" src/components/shared/LandingView.vue returns a match
    - grep "useI18n" src/components/shared/LandingView.vue returns a match
    - grep "t(" src/components/shared/LandingView.vue returns at least 5 matches (title, subtitle, 3 steps, cta)
    - grep -c "v-html" src/components/shared/LandingView.vue returns 0 (no v-html — no XSS surface)
    - grep "print:hidden" src/components/shared/LandingView.vue returns a match
    - grep "aria-hidden" src/components/shared/LandingView.vue returns a match
    - rtk npm run type-check exits 0
  </acceptance_criteria>
  <done>LandingView.vue component exists with full i18n support. All 4 locale files contain the landing.* namespace (en, fr, de, it). Component uses dismissLanding() from uiStore. All text rendered via t() calls. No v-html usage. Accessible (button element for CTA, aria-hidden on decorative badges). Type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate LandingView into App.vue and wire main.ts URL bypass</name>
  <files>src/App.vue, src/main.ts</files>
  <read_first>
    - src/App.vue (current template structure — 76 lines)
    - src/main.ts (current URL hydration block — 23 lines)
    - src/components/shared/LandingView.vue (just created in Task 1)
    - src/stores/uiStore.ts (confirm isLandingVisible and dismissLanding exports)
  </read_first>
  <action>
    **Step 1: Modify src/App.vue**

    1. Add import for LandingView in the `<script setup>` block, after the existing component imports:
       ```typescript
       import LandingView from '@/components/shared/LandingView.vue'
       ```

    2. Restructure the `<template>` to wrap the `<main>` content (NOT the header) with `v-if/v-else` on `ui.isLandingVisible`:

       The header stays OUTSIDE the conditional — always visible. The `<main>` is replaced by either LandingView or the two-pane wizard layout.

       New template structure (the header `<header>` block is UNCHANGED):
       ```html
       <template>
         <div class="min-h-screen bg-gray-50 dark:bg-gray-950 font-sans">
           <header class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700 px-6 py-3 flex items-center justify-between sticky top-0 z-10 print:hidden">
             <h1 class="text-lg font-bold text-gray-900 dark:text-gray-100">{{ t('app.title') }}</h1>
             <LanguageSwitcher />
           </header>

           <LandingView v-if="ui.isLandingVisible" />

           <main v-else class="grid grid-cols-1 md:grid-cols-2 min-h-[calc(100vh-56px)] print:grid-cols-1 print:min-h-0">
             <!-- LEFT PANE: wizard steps — UNCHANGED -->
             <!-- RIGHT PANE: results — UNCHANGED -->
           </main>
         </div>
       </template>
       ```

       CRITICAL: Use `v-if` / `v-else` (not `v-show`) because the wizard panels inside `<main>` already use `v-show` for step content preservation. The landing view and wizard layout are mutually exclusive — only one should be in the DOM at a time.

       The inner content of `<main>` (left pane with WizardStepper, TopologySelector, etc. and right pane with ResultsPanel) remains COMPLETELY UNCHANGED.

    **Step 2: Modify src/main.ts**

    Add `uiStore.dismissLanding()` to the existing URL hydration block, immediately after `uiStore.confirmTopology()`:

    ```typescript
    if (window.location.search.includes('s=')) {
      const uiStore = useUiStore()
      uiStore.confirmTopology()
      uiStore.dismissLanding()  // Phase 18: bypass landing for URL-hydrated sessions
    }
    ```

    This ensures that users arriving via a shared URL (containing `?s=...`) never see the landing view — they land directly on step 1 with their configuration hydrated.
  </action>
  <verify>
    <automated>rtk vitest run && rtk npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - grep "import LandingView" src/App.vue returns a match
    - grep "LandingView" src/App.vue returns at least 2 matches (import + template usage)
    - grep "v-if=\"ui.isLandingVisible\"" src/App.vue returns a match
    - grep "v-else" src/App.vue returns a match (on the main element)
    - grep "dismissLanding" src/main.ts returns a match
    - grep "confirmTopology" src/main.ts returns a match (existing — still present)
    - The header element is NOT inside any v-if/v-else block — grep confirms header is still present unconditionally
    - rtk vitest run exits 0 (all tests pass, no regressions)
    - rtk npm run type-check exits 0
    - rtk npm run build exits 0 (production build succeeds)
  </acceptance_criteria>
  <done>App.vue conditionally renders LandingView (v-if) or wizard layout (v-else) based on ui.isLandingVisible. Header remains always visible. main.ts calls dismissLanding() on URL hydration so shared URLs bypass the landing. All tests pass. Type check and build succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify landing view and wizard click-back navigation</name>
  <files>src/components/shared/LandingView.vue, src/App.vue, src/main.ts, src/components/shared/WizardStepper.vue</files>
  <action>
    Human verification checkpoint. No code changes in this task. Start the dev server with `npm run dev` and verify the following manually:

    **Landing View (WIZARD-02):**
    1. Open http://localhost:5173/vcf-sizer/ in a fresh browser tab (no ?s= param)
    2. Verify: Landing view is shown with tagline, 3-step summary cards, and "Start Sizing" button
    3. Verify: The sticky header with "VCF 9.x Sizing Calculator" title and language switcher is visible above the landing
    4. Switch locale to FR using the language switcher — verify landing text changes to French
    5. Click "Start Sizing" (or locale equivalent) — verify landing disappears and wizard step 1 (Topology) is shown

    **URL Bypass (WIZARD-02 success criterion 3):**
    6. Complete a full configuration (select topology, configure management, add a workload)
    7. Copy the URL from the address bar (it should contain ?s=...)
    8. Open that URL in a new tab — verify landing view does NOT appear; wizard loads directly at step 1 with configuration hydrated

    **Click-Back Navigation (WIZARD-01):**
    9. From step 1, select a topology (e.g., HA) — "Next" button becomes enabled
    10. Click "Next" to go to step 2 — step 1 badge turns green
    11. Click the green step 1 badge — verify you navigate back to step 1 content
    12. Click the grey step 3 badge — verify nothing happens (forward jump blocked)
    13. Go forward to step 2 again, then to step 3 — both step 1 and step 2 badges are green
    14. Click step 1 green badge — verify jump back to step 1 works
    15. Click step 2 green badge — verify jump back to step 2 works
    16. Verify all previously entered data is preserved after navigating back and forth

    Resume signal: Type "approved" or describe any issues found.
  </action>
  <verify>
    <automated>rtk npm run build</automated>
  </verify>
  <done>Human approves all 5 phase success criteria: (1) click completed step to jump back, (2) landing view on first load, (3) URL bypass for shared links, (4) forward jump blocked, (5) uiStore exposes chartImages and isLandingVisible APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n strings -> DOM | Landing view renders i18n translated strings into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Spoofing | LandingView i18n | accept | All strings come from bundled locale JSON files, not user input. Vue template interpolation (`{{ }}`) auto-escapes HTML. No v-html used. No XSS vector. |
| T-18-04 | Tampering | main.ts URL check | accept | `window.location.search.includes('s=')` is a read-only check on the URL. Malicious `s=` values are handled by Zod schema validation in `hydrateFromUrl()` which strips unknown keys and applies defaults. Landing bypass on invalid URLs is harmless — user simply sees the wizard with default state. |
| T-18-05 | Denial of Service | LandingView rendering | accept | LandingView is a static template with no loops over user data, no network calls, no heavy computation. Cannot be used for resource exhaustion. |
</threat_model>

<verification>
1. `rtk vitest run` — full suite green (271+ tests, no regressions)
2. `rtk npm run type-check` — zero TypeScript errors
3. `rtk npm run build` — production build succeeds
4. Manual: fresh load shows landing view with correct i18n text
5. Manual: locale switch works on landing view (header LanguageSwitcher accessible)
6. Manual: "Start Sizing" dismisses landing, shows wizard
7. Manual: shared URL (?s=...) bypasses landing entirely
8. Manual: completed step badges are clickable; future steps are not
</verification>

<success_criteria>
- LandingView.vue exists with full i18n support across all 4 locales
- App.vue conditionally renders LandingView vs wizard based on isLandingVisible
- Header (with LanguageSwitcher) remains visible during landing view
- main.ts bypasses landing for URL-hydrated sessions
- All 271+ tests pass, type check passes, production build succeeds
- Human verification confirms all 5 phase success criteria met
</success_criteria>

<output>
After completion, create `.planning/phases/18-store-foundations-wizard-navigation/18-02-SUMMARY.md`
</output>
