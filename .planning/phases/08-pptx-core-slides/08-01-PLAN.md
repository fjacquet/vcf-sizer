---
phase: 08-pptx-core-slides
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/composables/usePptxExport.test.ts
autonomous: true
requirements:
  - PPTX-01
  - PPTX-02
  - PPTX-03
  - PPTX-04
  - PPTX-05
  - PPTX-06
  - PPTX-07
  - PPTX-08
  - PPTX-09

must_haves:
  truths:
    - "Test file imports buildTitleSlideData, buildConfigSummaryData, buildWorkloadSlideData, buildMgmtOverheadData, buildComputeResultsData, buildStorageResultsData, buildRecommendationsData from usePptxExport"
    - "Each data-mapping helper has at least one test asserting its return shape"
    - "PPTX_MASTER_COLOR constant is asserted to equal '003087'"
    - "All tests fail because usePptxExport.ts does not exist yet (RED state)"
  artifacts:
    - path: "src/composables/usePptxExport.test.ts"
      provides: "Failing test suite for all PPTX data-mapping helpers"
      min_lines: 120
  key_links:
    - from: "src/composables/usePptxExport.test.ts"
      to: "src/composables/usePptxExport.ts"
      via: "import { buildTitleSlideData, ... } from './usePptxExport'"
      pattern: "from './usePptxExport'"
---

<objective>
Create the failing test file for all PPTX data-mapping helper functions (TDD Wave 0 gate).

Purpose: Establish RED tests before any implementation exists, ensuring every data-mapping function has test coverage before it is written.
Output: `src/composables/usePptxExport.test.ts` with tests for 7 slide data builders + master color constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/composables/useMarkdownExport.test.ts
@src/stores/inputStore.ts
@src/stores/calculationStore.ts
@src/engine/types.ts

<interfaces>
<!-- Key types the executor needs for writing test assertions -->

From src/engine/types.ts:
```typescript
export interface MgmtDomainResult {
  vcenterCores: number; vcenterRamGB: number
  sddcCores: number; sddcRamGB: number
  nsxCores: number; nsxRamGB: number
  opsCores: number; opsRamGB: number
  automationCores: number; automationRamGB: number
  totalCores: number; totalRamGB: number
}

export interface ComputeResult {
  totalCoresRequired: number; totalRamRequiredGB: number
  availableCores: number; availableRamGB: number
  coreUtilizationPct: number; ramUtilizationPct: number
  minHostsForCpu: number; minHostsForRam: number
  recommendedHostCount: number
}

export interface StorageResult {
  rawCapacityTB: number; raidMultiplier: number
  usableAfterRaidTB: number; lfsOverheadTB: number
  metadataOverheadTB: number; usableBeforeDedupTB: number
  effectiveCapacityTB: number; safeUsableCapacityTB: number
  raidScheme: string; minHostsRequired: number
}
```

From src/stores/inputStore.ts (default values):
```typescript
deploymentMode: 'ha', hostCount: 4, coresPerSocket: 16, socketsPerHost: 2,
hostRamGB: 512, hostStorageTB: 3.84, storageType: 'vsan-esa',
networkSpeedGbE: 25, managementArchitecture: 'shared',
vmCount: 100, avgVcpuPerVm: 4, avgVramGbPerVm: 8,
avgStorageGbPerVm: 100, cpuOvercommitRatio: 4, ramOvercommitRatio: 1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test file for PPTX data-mapping helpers</name>
  <files>src/composables/usePptxExport.test.ts</files>
  <read_first>
    - src/composables/useMarkdownExport.test.ts
    - src/stores/inputStore.ts
    - src/stores/calculationStore.ts
    - src/engine/types.ts
  </read_first>
  <behavior>
    - PPTX_MASTER_COLOR exported constant equals '003087' (PPTX-02)
    - buildTitleSlideData returns object with deploymentMode and date string (PPTX-03)
    - buildConfigSummaryData returns array of rows containing hostCount, coresPerSocket, socketsPerHost, hostRamGB, hostStorageTB, storageType, networkSpeedGbE, managementArchitecture (PPTX-04)
    - buildWorkloadSlideData returns array of rows containing vmCount, avgVcpuPerVm, avgVramGbPerVm, avgStorageGbPerVm, cpuOvercommitRatio, ramOvercommitRatio (PPTX-05)
    - buildMgmtOverheadData returns array with 6 component rows (vCenter, SDDC, NSX, Ops, Automation, Total) each having label, cores, ramGB (PPTX-06)
    - buildComputeResultsData returns object with recommendedHostCount, coreUtilizationPct, ramUtilizationPct (PPTX-07)
    - buildStorageResultsData returns object with rawCapacityTB, usableAfterRaidTB, lfsOverheadTB, metadataOverheadTB, safeUsableCapacityTB (PPTX-08)
    - buildRecommendationsData returns array of recommendation strings, non-empty (PPTX-09)
    - generatePptxReport is exported as an async function (PPTX-01)
  </behavior>
  <action>
Create `src/composables/usePptxExport.test.ts` following the `useMarkdownExport.test.ts` pattern:

1. Import `createPinia`, `setActivePinia` from `pinia`
2. Import `useInputStore` from `@/stores/inputStore` and `useCalculationStore` from `@/stores/calculationStore`
3. Import from `./usePptxExport`:
   - `PPTX_MASTER_COLOR` (constant)
   - `buildTitleSlideData`
   - `buildConfigSummaryData`
   - `buildWorkloadSlideData`
   - `buildMgmtOverheadData`
   - `buildComputeResultsData`
   - `buildStorageResultsData`
   - `buildRecommendationsData`
   - `generatePptxReport`

4. Use `beforeEach(() => { setActivePinia(createPinia()) })` in each describe block

5. Write these describe blocks with tests:

**describe('PPTX_MASTER_COLOR constant — PPTX-02')**
- `it('equals 003087')` — `expect(PPTX_MASTER_COLOR).toBe('003087')`

**describe('buildTitleSlideData — PPTX-03')**
- `it('returns deploymentMode from input store')` — create store, call `buildTitleSlideData(store)`, expect result.deploymentMode to be `'ha'` (default)
- `it('returns a date string in YYYY-MM-DD format')` — expect result.date to match `/^\d{4}-\d{2}-\d{2}$/`

**describe('buildConfigSummaryData — PPTX-04')**
- `it('returns rows containing all 8 config fields')` — create stores, call `buildConfigSummaryData(store)`, assert result is array with >= 8 items, each item has `label` and `value` properties
- `it('hostCount value matches store default 4')` — find row with label containing 'Host' and value containing '4'

**describe('buildWorkloadSlideData — PPTX-05')**
- `it('returns rows for vmCount, vCPU, vRAM, storage, CPU ratio, RAM ratio')` — call `buildWorkloadSlideData(store)`, assert length >= 6
- `it('vmCount row value matches store default 100')` — find row with value containing '100'

**describe('buildMgmtOverheadData — PPTX-06')**
- `it('returns 6 rows (vCenter, SDDC, NSX, Ops, Automation, Total)')` — call `buildMgmtOverheadData(calc.management)`, assert length === 6
- `it('each row has label, cores, ramGB')` — assert each item has all three keys
- `it('Total row cores matches management.totalCores')` — find Total row, compare

**describe('buildComputeResultsData — PPTX-07')**
- `it('returns recommendedHostCount as a number')` — call `buildComputeResultsData(calc.compute)`, assert typeof result.recommendedHostCount === 'number'
- `it('returns coreUtilizationPct and ramUtilizationPct')` — assert both exist and are numbers

**describe('buildStorageResultsData — PPTX-08')**
- `it('returns safeUsableCapacityTB as a number')` — call `buildStorageResultsData(calc.storage)`, assert typeof result.safeUsableCapacityTB === 'number'
- `it('returns rawCapacityTB, lfsOverheadTB, metadataOverheadTB')` — assert all exist

**describe('buildRecommendationsData — PPTX-09')**
- `it('returns a non-empty array of strings')` — call `buildRecommendationsData(store, calc)`, assert Array.isArray, length > 0, each item typeof 'string'

**describe('generatePptxReport — PPTX-01')**
- `it('is an async function')` — `expect(typeof generatePptxReport).toBe('function')`

IMPORTANT: For helpers that take store/calc data, pass the actual Pinia store instances. The helpers will accept the store objects directly (they are snapshot readers, not Vue-reactive consumers). The store fields in the test will have default values from inputStore.ts.

This test file WILL FAIL (RED state) because `usePptxExport.ts` does not exist yet. That is the expected outcome for Wave 0.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && npx vitest run src/composables/usePptxExport.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/composables/usePptxExport.test.ts` exists
    - File contains `from './usePptxExport'`
    - File contains `PPTX_MASTER_COLOR`
    - File contains `buildTitleSlideData`
    - File contains `buildConfigSummaryData`
    - File contains `buildWorkloadSlideData`
    - File contains `buildMgmtOverheadData`
    - File contains `buildComputeResultsData`
    - File contains `buildStorageResultsData`
    - File contains `buildRecommendationsData`
    - File contains `generatePptxReport`
    - File contains `createPinia`
    - Test command exits non-zero (RED state — import fails because usePptxExport.ts does not exist)
  </acceptance_criteria>
  <done>Test file exists with 15+ test cases covering all 7 data-mapping helpers + master color constant + generatePptxReport type check. All tests fail because the source module does not exist yet.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/composables/usePptxExport.test.ts` exits non-zero (expected RED)
- File contains at least 15 `it(` blocks
- All 7 builder function names appear in imports
</verification>

<success_criteria>
- Test file committed with all data-mapping assertions
- Tests reference real store default values (vmCount=100, hostCount=4, etc.)
- RED state confirmed (import error from missing source file)
</success_criteria>

<output>
After completion, create `.planning/phases/08-pptx-core-slides/08-01-SUMMARY.md`
</output>
