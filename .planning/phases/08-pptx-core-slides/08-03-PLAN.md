---
phase: 08-pptx-core-slides
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - src/components/results/ExportToolbar.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - PPTX-01

must_haves:
  truths:
    - "A 'Download PPTX' button is visible in ExportToolbar alongside the existing Share, Markdown, and Print buttons"
    - "Clicking the PPTX button triggers generatePptxReport() and shows a loading state while generating"
    - "The button label uses i18n keys results.toolbar.exportPptx / results.toolbar.exportPptxLoading in all 4 locales"
  artifacts:
    - path: "src/components/results/ExportToolbar.vue"
      provides: "PPTX download button with async handler"
      contains: "handleExportPptx"
    - path: "src/i18n/locales/en.json"
      provides: "exportPptx and exportPptxLoading keys"
      contains: "exportPptx"
  key_links:
    - from: "src/components/results/ExportToolbar.vue"
      to: "src/composables/usePptxExport.ts"
      via: "import { generatePptxReport } from '@/composables/usePptxExport'"
      pattern: "generatePptxReport.*usePptxExport"
---

<objective>
Wire the PPTX export button into ExportToolbar.vue and add i18n keys to all 4 locale files.

Purpose: Make the PPTX download accessible to users through the existing export toolbar UI.
Output: ExportToolbar.vue with a functioning "Download PPTX" button; all 4 locale files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pptx-core-slides/08-02-SUMMARY.md
@src/components/results/ExportToolbar.vue
@src/composables/usePptxExport.ts

<interfaces>
From src/composables/usePptxExport.ts:
```typescript
export async function generatePptxReport(): Promise<void>
```

From src/components/results/ExportToolbar.vue (current buttons):
```html
<!-- Share URL, Export Markdown, Print / PDF -->
<!-- Button class: "px-3 py-1.5 text-sm font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors" -->
```

From src/i18n/locales/en.json (current toolbar keys):
```json
"toolbar": {
  "share": "Share URL",
  "copied": "Copied!",
  "exportMd": "Export Markdown",
  "print": "Print / PDF"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PPTX button to ExportToolbar.vue and i18n keys to all 4 locales</name>
  <files>src/components/results/ExportToolbar.vue, src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>
    - src/components/results/ExportToolbar.vue
    - src/i18n/locales/en.json
    - src/i18n/locales/fr.json
    - src/i18n/locales/de.json
    - src/i18n/locales/it.json
  </read_first>
  <action>
**ExportToolbar.vue changes:**

1. Add import at top of `<script setup>`:
```typescript
import { generatePptxReport } from '@/composables/usePptxExport'
```

2. Add reactive state and handler:
```typescript
const pptxLoading = ref(false)

async function handleExportPptx() {
  pptxLoading.value = true
  try {
    await generatePptxReport()
  } finally {
    pptxLoading.value = false
  }
}
```

3. Add button in template AFTER the Print/PDF button, inside the existing flex container:
```html
<button
  class="px-3 py-1.5 text-sm font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors disabled:opacity-50"
  :disabled="pptxLoading"
  @click="handleExportPptx"
>
  {{ pptxLoading ? t('results.toolbar.exportPptxLoading') : t('results.toolbar.exportPptx') }}
</button>
```

Note the `disabled:opacity-50` class added for loading state visual feedback and `:disabled="pptxLoading"` attribute.

**i18n locale file changes:**

Add two keys to `results.toolbar` in each locale file:

`en.json`:
```json
"exportPptx": "Download PPTX",
"exportPptxLoading": "Generating..."
```

`fr.json`:
```json
"exportPptx": "Exporter PPTX",
"exportPptxLoading": "Generation..."
```

`de.json`:
```json
"exportPptx": "PPTX herunterladen",
"exportPptxLoading": "Wird erstellt..."
```

`it.json`:
```json
"exportPptx": "Scarica PPTX",
"exportPptxLoading": "Generazione..."
```
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && npm run build 2>&1 | tail -3 && grep -c "handleExportPptx" src/components/results/ExportToolbar.vue && grep -c "exportPptx" src/i18n/locales/en.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/ExportToolbar.vue` contains `import { generatePptxReport } from '@/composables/usePptxExport'`
    - `src/components/results/ExportToolbar.vue` contains `handleExportPptx`
    - `src/components/results/ExportToolbar.vue` contains `pptxLoading`
    - `src/components/results/ExportToolbar.vue` contains `results.toolbar.exportPptx`
    - `src/i18n/locales/en.json` contains `"exportPptx"`
    - `src/i18n/locales/en.json` contains `"exportPptxLoading"`
    - `src/i18n/locales/fr.json` contains `"exportPptx"`
    - `src/i18n/locales/de.json` contains `"exportPptx"`
    - `src/i18n/locales/it.json` contains `"exportPptx"`
    - `npm run build` exits zero
    - `npm run test` exits zero (no regressions)
  </acceptance_criteria>
  <done>PPTX download button visible in toolbar with loading state. All 4 locales have the new i18n keys. Build and full test suite pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PPTX export end-to-end</name>
  <files>src/components/results/ExportToolbar.vue</files>
  <action>
Human verifies the complete PPTX export feature end-to-end. No code changes in this task — purely visual and functional verification of the .pptx output in a presentation application.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && npm run build 2>&1 | tail -1</automated>
  </verify>
  <what-built>Complete PPTX export feature: button in toolbar triggers download of a .pptx file with 7 VCF-branded slides (title, config summary, workload, mgmt overhead, compute, storage, recommendations).</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:5173/vcf-sizer/
2. Verify "Download PPTX" button appears in the export toolbar (next to Share URL, Export Markdown, Print/PDF)
3. Click "Download PPTX" — button should show "Generating..." briefly, then trigger a file download
4. Confirm `vcf-sizing-report.pptx` is downloaded
5. Open the .pptx file in PowerPoint, Keynote, or LibreOffice Impress
6. Verify exactly 7 slides:
   - Slide 1: Title with "VCF 9.x Sizing Report", deployment mode "ha", and today's date
   - Slide 2: Configuration Summary table (8 rows: hosts, cores, sockets, RAM, storage, type, network, mgmt arch)
   - Slide 3: Workload Profile table (6 rows: VM count, vCPU, vRAM, storage, CPU ratio, RAM ratio)
   - Slide 4: Management Domain Overhead table (6 rows: vCenter, SDDC, NSX, Ops, Automation, Total)
   - Slide 5: Compute Results table (host count, CPU/RAM utilization)
   - Slide 6: Storage Results table (raw, RAID, LFS, metadata, safe usable)
   - Slide 7: Recommendations bullet list
7. Verify all slides have dark blue (#003087) background with footer bar
8. Change some input values and export again — verify the new .pptx reflects updated values
9. Switch locale to French — verify button shows "Exporter PPTX"
  </how-to-verify>
  <done>User confirms: PPTX downloads successfully, opens in presentation software with 7 branded slides, data matches current store state, button label is localized.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits zero
- `npm run test` exits zero (full suite, no regressions)
- ExportToolbar.vue contains 4 buttons (share, markdown, print, pptx)
- All 4 locale files contain exportPptx key
- Manual: .pptx file opens with 7 branded slides
</verification>

<success_criteria>
- "Download PPTX" button visible and functional in all 4 locales
- Downloaded .pptx contains exactly 7 slides with correct content
- Slide master uses Broadcom blue background
- No impact on existing Share, Markdown, or Print functionality
- Build and test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-pptx-core-slides/08-03-SUMMARY.md`
</output>
