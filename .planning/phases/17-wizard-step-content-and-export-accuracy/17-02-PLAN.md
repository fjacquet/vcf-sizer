---
phase: 17
plan: 2
slug: wizard-ui-components
wave: 2
requires: [1]
autonomous: true
status: pending
created: 2026-03-31
requirements: [WIZARD-05, WIZARD-06]
files_modified:
  - src/components/shared/ManagementResultCard.vue
  - src/components/shared/ManagementCommittedSummary.vue
  - src/App.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
must_haves:
  truths:
    - "Step 2 bottom shows management host count and resource figures"
    - "Step 3 top has a collapsed panel showing committed management resources"
    - "Collapsed panel in step 3 is read-only -- no input components"
    - "All new UI text uses i18n keys present in all 4 locales"
  artifacts:
    - path: "src/components/shared/ManagementResultCard.vue"
      provides: "Management result card for step 2"
      min_lines: 20
    - path: "src/components/shared/ManagementCommittedSummary.vue"
      provides: "Collapsed management summary for step 3"
      min_lines: 20
    - path: "src/App.vue"
      provides: "Integration of both new components into wizard step panels"
      contains: "ManagementResultCard"
  key_links:
    - from: "src/components/shared/ManagementResultCard.vue"
      to: "src/stores/calculationStore.ts"
      via: "calc.dedicatedMgmtHostCount, calc.management"
      pattern: "dedicatedMgmtHostCount"
    - from: "src/components/shared/ManagementCommittedSummary.vue"
      to: "src/stores/calculationStore.ts"
      via: "calc.dedicatedMgmtHostCount, calc.management"
      pattern: "management\\.totalCores"
    - from: "src/App.vue"
      to: "src/components/shared/ManagementResultCard.vue"
      via: "component import and placement in step 2 panel"
      pattern: "ManagementResultCard"
---

<objective>
Create ManagementResultCard (step 2 bottom) and ManagementCommittedSummary (step 3 top) components, integrate into App.vue, and add all i18n keys for all 4 locales.

Purpose: Users see management sizing results before advancing to workloads (step 2) and can review committed management resources during workload configuration (step 3).
Output: Two new Vue components, App.vue integration, i18n keys in en/fr/de/it.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-wizard-step-content-and-export-accuracy/17-RESEARCH.md

@src/App.vue
@src/stores/calculationStore.ts
@src/stores/inputStore.ts
@src/i18n/locales/en.json

<interfaces>
From src/stores/calculationStore.ts (relevant computed values):
  dedicatedMgmtHostCount: number | null  // null when colocated
  management: MgmtDomainResult {
    totalCores: number
    totalRamGB: number
    vcenterCores, vcenterRamGB, sddcCores, sddcRamGB, nsxCores, nsxRamGB,
    opsCores, opsRamGB, automationCores, automationRamGB
  }

From src/stores/inputStore.ts:
  managementArchitecture: 'colocated' | 'dedicated'

From src/App.vue step panels:
  Step 2 panel: v-show="ui.currentWizardStep === 2" contains ManagementDomainSection + ManagementSummary
  Step 3 panel: v-show="ui.currentWizardStep === 3" contains DomainTabStrip + forms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManagementResultCard.vue and ManagementCommittedSummary.vue</name>
  <files>src/components/shared/ManagementResultCard.vue, src/components/shared/ManagementCommittedSummary.vue</files>
  <action>
    **ManagementResultCard.vue** (WIZARD-05) -- placed at bottom of step 2:
    - `<script setup lang="ts">` with imports: `computed` from vue, `useI18n`, `useInputStore`, `useCalculationStore`
    - Compute `hostCountDisplay`:
      - dedicated: `String(calc.dedicatedMgmtHostCount ?? 0)`
      - colocated: `t('wizard.mgmtResult.colocatedLabel')` (e.g., "Colocated with WLD-1")
    - Compute `architectureLabel`: `t('wizard.mgmtResult.dedicated')` or `t('wizard.mgmtResult.colocated')`
    - Template: card with border, rounded, padding (match existing card style in the project):
      ```html
      <section class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4 space-y-2">
        <h3 class="text-sm font-semibold text-gray-900 dark:text-gray-100">{{ t('wizard.mgmtResult.title') }}</h3>
        <div class="grid grid-cols-3 gap-4 text-sm">
          <div>
            <p class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.hostCount') }}</p>
            <p class="font-medium text-gray-900 dark:text-gray-100">{{ hostCountDisplay }}</p>
          </div>
          <div>
            <p class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.vcpu') }}</p>
            <p class="font-medium text-gray-900 dark:text-gray-100">{{ calc.management.totalCores }}</p>
          </div>
          <div>
            <p class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.ram') }}</p>
            <p class="font-medium text-gray-900 dark:text-gray-100">{{ calc.management.totalRamGB }} GB</p>
          </div>
        </div>
      </section>
      ```

    **ManagementCommittedSummary.vue** (WIZARD-06) -- collapsed panel at top of step 3:
    - `<script setup lang="ts">` with imports: `ref`, `computed`, `useI18n`, `useInputStore`, `useCalculationStore`
    - `const isExpanded = ref(false)` -- collapsed by default
    - Same host count display logic as ManagementResultCard
    - Template: collapsible section using a button toggle + v-show:
      ```html
      <section class="bg-gray-50 dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700">
        <button
          @click="isExpanded = !isExpanded"
          class="w-full flex items-center justify-between px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg"
        >
          <span>{{ t('wizard.mgmtCommitted.title') }}</span>
          <span class="text-xs">{{ isExpanded ? '&#9650;' : '&#9660;' }}</span>
        </button>
        <div v-show="isExpanded" class="px-4 pb-3 space-y-1 text-sm">
          <div class="flex justify-between">
            <span class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.hostCount') }}</span>
            <span class="font-medium text-gray-900 dark:text-gray-100">{{ hostCountDisplay }}</span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.vcpu') }}</span>
            <span class="font-medium text-gray-900 dark:text-gray-100">{{ calc.management.totalCores }}</span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-500 dark:text-gray-400">{{ t('wizard.mgmtResult.ram') }}</span>
            <span class="font-medium text-gray-900 dark:text-gray-100">{{ calc.management.totalRamGB }} GB</span>
          </div>
        </div>
      </section>
      ```
    - CONSTRAINT: This component is READ-ONLY. No input components, no buttons that mutate store state (except the expand toggle).
    - Use `v-show` (not `v-if`) for the expanded content -- data preservation per WIZARD-02 convention.
  </action>
  <verify>
    <automated>npm run build</automated>
    Build succeeds -- both new components compile without type errors.
  </verify>
  <done>
    - ManagementResultCard.vue exists showing host count (dedicated number or "colocated with WLD-1"), vCPU required, RAM required
    - ManagementCommittedSummary.vue exists with collapsed-by-default toggle, read-only display of same values
    - Both components compile with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate components into App.vue and add all i18n keys to 4 locales</name>
  <files>src/App.vue, src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <action>
    **App.vue** -- Add imports and place components:
    - Import: `import ManagementResultCard from '@/components/shared/ManagementResultCard.vue'`
    - Import: `import ManagementCommittedSummary from '@/components/shared/ManagementCommittedSummary.vue'`
    - In step 2 panel (`v-show="ui.currentWizardStep === 2"`), add `<ManagementResultCard />` AFTER `<ManagementSummary />` (at bottom of step 2)
    - In step 3 panel (`v-show="ui.currentWizardStep === 3"`), add `<ManagementCommittedSummary />` BEFORE `<DomainTabStrip />` (at top of step 3)

    **i18n keys** -- Add to the `wizard` section of ALL 4 locale files. New keys:

    en.json:
    ```json
    "wizard": {
      "step1": { "number": "1", "label": "Topology", "topologyRequired": "Select a deployment topology to continue" },
      "step2": { "number": "2", "label": "Management", "mgmtInvalid": "Configure a valid management domain to continue" },
      "step3": { "number": "3", "label": "Workloads" },
      "nav": { "previous": "Previous", "next": "Next" },
      "mgmtResult": {
        "title": "Management Domain Result",
        "hostCount": "Host Count",
        "vcpu": "vCPU Required",
        "ram": "RAM Required",
        "dedicated": "Dedicated",
        "colocated": "Colocated",
        "colocatedLabel": "Colocated with WLD-1"
      },
      "mgmtCommitted": {
        "title": "Management Domain (committed)"
      }
    }
    ```

    fr.json (Swiss French):
    ```json
    "wizard": {
      "step1": { "number": "1", "label": "Topologie", "topologyRequired": "Choisissez une topologie pour continuer" },
      "step2": { "number": "2", "label": "Gestion", "mgmtInvalid": "Configurez un domaine de gestion valide pour continuer" },
      "step3": { "number": "3", "label": "Charges de travail" },
      "nav": { "previous": "Retour", "next": "Suivant" },
      "mgmtResult": {
        "title": "Resultat du domaine de gestion",
        "hostCount": "Nombre d'hotes",
        "vcpu": "vCPU requis",
        "ram": "RAM requise",
        "dedicated": "Dedie",
        "colocated": "Colocalise",
        "colocatedLabel": "Colocalise avec WLD-1"
      },
      "mgmtCommitted": {
        "title": "Domaine de gestion (valide)"
      }
    }
    ```

    de.json (Swiss German):
    ```json
    "wizard": {
      "step1": { "number": "1", "label": "Topologie", "topologyRequired": "Topologie auswahlen, um fortzufahren" },
      "step2": { "number": "2", "label": "Management", "mgmtInvalid": "Gultige Management-Domane konfigurieren" },
      "step3": { "number": "3", "label": "Workloads" },
      "nav": { "previous": "Zuruck", "next": "Weiter" },
      "mgmtResult": {
        "title": "Management-Domane Ergebnis",
        "hostCount": "Anzahl Hosts",
        "vcpu": "vCPU erforderlich",
        "ram": "RAM erforderlich",
        "dedicated": "Dediziert",
        "colocated": "Kolokalisiert",
        "colocatedLabel": "Kolokalisiert mit WLD-1"
      },
      "mgmtCommitted": {
        "title": "Management-Domane (festgelegt)"
      }
    }
    ```

    it.json (Swiss Italian):
    ```json
    "wizard": {
      "step1": { "number": "1", "label": "Topologia", "topologyRequired": "Seleziona una topologia per continuare" },
      "step2": { "number": "2", "label": "Gestione", "mgmtInvalid": "Configura un dominio di gestione valido per continuare" },
      "step3": { "number": "3", "label": "Carichi di lavoro" },
      "nav": { "previous": "Indietro", "next": "Avanti" },
      "mgmtResult": {
        "title": "Risultato dominio di gestione",
        "hostCount": "Conteggio host",
        "vcpu": "vCPU richiesti",
        "ram": "RAM richiesta",
        "dedicated": "Dedicato",
        "colocated": "Colocalizzato",
        "colocatedLabel": "Colocalizzato con WLD-1"
      },
      "mgmtCommitted": {
        "title": "Dominio di gestione (confermato)"
      }
    }
    ```

    IMPORTANT: Merge these keys into the EXISTING wizard section in each file. Do not delete existing keys. Add the new nested keys (`topologyRequired`, `mgmtInvalid`, `mgmtResult.*`, `mgmtCommitted.*`) alongside existing ones.
  </action>
  <verify>
    <automated>npm run build && npm run test</automated>
    Build succeeds. All tests pass. No missing i18n key warnings.
  </verify>
  <done>
    - ManagementResultCard visible at bottom of step 2 panel in App.vue
    - ManagementCommittedSummary visible at top of step 3 panel in App.vue (collapsed by default)
    - All i18n keys present in en.json, fr.json, de.json, it.json
    - Build and tests pass
  </done>
</task>

</tasks>

<verification>
- `npm run build` -- zero type errors
- `npm run test` -- full suite passes
- Manual: dev server step 2 shows ManagementResultCard at bottom
- Manual: dev server step 3 shows collapsed ManagementCommittedSummary at top
</verification>

<success_criteria>
1. ManagementResultCard shows dedicated host count OR "colocated with WLD-1", management vCPU, management RAM (WIZARD-05)
2. ManagementCommittedSummary collapsed by default with toggle, read-only display (WIZARD-06)
3. Both components placed correctly in App.vue step panels
4. All i18n keys present in all 4 locale files
5. Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-wizard-step-content-and-export-accuracy/17-02-SUMMARY.md`
</output>
