---
phase: 04-correctness-and-architecture-validation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/input/DeploymentModelSelector.vue
  - src/components/results/StretchNetworkChecklist.vue
  - src/components/results/ResultsPanel.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements:
  - STRCH-06
  - STRCH-07
  - STRCH-08
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "User sees a minimum 10 Gbps bandwidth recommendation for any stretch cluster configuration"
    - "User sees a visible indicator when the bandwidth floor has been applied rather than the formula result"
    - "User sees a stretch network checklist with MTU 9000, RTT < 5ms, and witness RTT threshold when stretch mode is active"
    - "User can toggle Dedicated Domains in HA or Stretch mode and sees an error when fewer than 4 management hosts"
    - "User in co-located mode with below-minimum hosts sees an informational note"
    - "All new UI text renders in all four Swiss locales (en, fr, de, it)"
    - "Network checklist is NOT visible when deployment mode is simple or ha"
    - "Management architecture toggle is NOT visible when deployment mode is simple"
  artifacts:
    - path: "src/components/results/StretchNetworkChecklist.vue"
      provides: "Stretch network checklist card with MTU, RTT, bandwidth values"
      min_lines: 30
    - path: "src/components/input/DeploymentModelSelector.vue"
      provides: "Management architecture toggle, bandwidth floor indicator, checklist integration"
      contains: "managementArchitecture"
    - path: "src/components/results/ResultsPanel.vue"
      provides: "StretchNetworkChecklist component integration"
      contains: "StretchNetworkChecklist"
    - path: "src/i18n/locales/en.json"
      provides: "English locale with all new keys"
      contains: "bandwidthFloorIndicator"
    - path: "src/i18n/locales/fr.json"
      provides: "French locale with all new keys"
      contains: "bandwidthFloorIndicator"
    - path: "src/i18n/locales/de.json"
      provides: "German locale with all new keys"
      contains: "bandwidthFloorIndicator"
    - path: "src/i18n/locales/it.json"
      provides: "Italian locale with all new keys"
      contains: "bandwidthFloorIndicator"
  key_links:
    - from: "src/components/input/DeploymentModelSelector.vue"
      to: "src/stores/inputStore.ts"
      via: "storeToRefs for managementArchitecture"
      pattern: "managementArchitecture"
    - from: "src/components/input/DeploymentModelSelector.vue"
      to: "src/stores/calculationStore.ts"
      via: "stretch.bandwidthFloorApplied, validationErrors"
      pattern: "bandwidthFloorApplied"
    - from: "src/components/results/StretchNetworkChecklist.vue"
      to: "src/stores/calculationStore.ts"
      via: "stretch.networkChecklist"
      pattern: "networkChecklist"
    - from: "src/components/results/ResultsPanel.vue"
      to: "src/components/results/StretchNetworkChecklist.vue"
      via: "component import and render"
      pattern: "StretchNetworkChecklist"
---

<objective>
Build all UI components and i18n keys for Phase 4: bandwidth floor indicator in DeploymentModelSelector, new StretchNetworkChecklist output component, management architecture toggle with validation display, and all four Swiss locale translations.

Purpose: Surface the engine correctness fixes and new validation rules from Plan 01 to the user through the existing UI component architecture.
Output: Updated DeploymentModelSelector with management toggle + floor indicator, new StretchNetworkChecklist card in results panel, all 4 locale files with new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-correctness-and-architecture-validation/04-RESEARCH.md
@.planning/phases/04-correctness-and-architecture-validation/04-01-SUMMARY.md

<interfaces>
<!-- Interfaces available after Plan 01 completes -->

From src/engine/types.ts (after Plan 01):
```typescript
export type ManagementArchitecture = 'shared' | 'dedicated'

export interface StretchNetworkChecklist {
  minInterSiteBandwidthGbps: number
  maxInterSiteLatencyMs: number
  maxWitnessLatencyMs: number
  jumboFramesRequired: boolean
  witnessMinBandwidthMbps: number
}

export interface StretchResult {
  totalHosts: number
  witnessCores: number
  witnessRamGB: number
  minBandwidthGbps: number
  effectivePerSiteStorageTB: number
  storageNote: string
  bandwidthFloorApplied: boolean
  networkChecklist: StretchNetworkChecklist
}
```

From src/stores/inputStore.ts (after Plan 01):
```typescript
// New ref:
const managementArchitecture = ref<'shared' | 'dedicated'>('shared')
// Returned in store
```

From src/stores/calculationStore.ts (after Plan 01):
```typescript
// New computed:
const dedicatedMgmtHostCount = computed<number | null>(...)
// Return: { management, compute, storage, validationErrors, stretch, dedicatedMgmtHostCount }
```

From src/components/shared/WarningBanner.vue:
```typescript
defineProps<{ message: string; severity?: 'error' | 'warning' }>()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all i18n keys to all 4 locale files</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>
    - src/i18n/locales/en.json (current key structure — understand nesting pattern)
    - src/i18n/locales/fr.json (current French translations — match style)
    - src/i18n/locales/de.json (current German translations — match style)
    - src/i18n/locales/it.json (current Italian translations — match style)
    - .planning/phases/04-correctness-and-architecture-validation/04-RESEARCH.md (Pattern 7 for key structure)
  </read_first>
  <action>
    Add the following keys to ALL FOUR locale files. Keys must be identical across files; only values change per language.

    ADD to the "deployment.stretchSites" object (after existing "storageNoteLabel" key):

    en.json:
    ```
    "bandwidthFloorIndicator": "10 Gbps floor applied (VCF 9.0 minimum -- formula result was below threshold)",
    "networkChecklist": {
      "title": "Stretch Network Requirements",
      "minBandwidth": "Min inter-site bandwidth",
      "maxLatency": "Max site-to-site RTT",
      "maxWitnessLatency": "Max witness RTT",
      "jumboFrames": "Jumbo frames (MTU 9000)",
      "required": "Required",
      "witnessBandwidth": "Witness min bandwidth",
      "highHostNote": "Consult Broadcom TechDocs for configurations above 15 hosts/site."
    }
    ```

    fr.json:
    ```
    "bandwidthFloorIndicator": "Seuil de 10 Gbps applique (minimum VCF 9.0 -- le resultat de la formule etait inferieur au seuil)",
    "networkChecklist": {
      "title": "Exigences reseau du cluster etendu",
      "minBandwidth": "Bande passante inter-site min.",
      "maxLatency": "RTT max site a site",
      "maxWitnessLatency": "RTT max temoin",
      "jumboFrames": "Trames jumbo (MTU 9000)",
      "required": "Requis",
      "witnessBandwidth": "Bande passante min. temoin",
      "highHostNote": "Consultez les TechDocs Broadcom pour les configurations de plus de 15 hotes/site."
    }
    ```

    de.json:
    ```
    "bandwidthFloorIndicator": "10-Gbps-Mindestgrenze angewandt (VCF 9.0 Minimum -- Formelergebnis lag unter dem Schwellenwert)",
    "networkChecklist": {
      "title": "Stretch-Netzwerkanforderungen",
      "minBandwidth": "Min. Bandbreite zwischen Standorten",
      "maxLatency": "Max. RTT zwischen Standorten",
      "maxWitnessLatency": "Max. Witness-RTT",
      "jumboFrames": "Jumbo-Frames (MTU 9000)",
      "required": "Erforderlich",
      "witnessBandwidth": "Min. Witness-Bandbreite",
      "highHostNote": "Weitere Informationen zu Konfigurationen mit mehr als 15 Hosts/Standort finden Sie in den Broadcom TechDocs."
    }
    ```

    it.json:
    ```
    "bandwidthFloorIndicator": "Soglia minima di 10 Gbps applicata (minimo VCF 9.0 -- il risultato della formula era inferiore alla soglia)",
    "networkChecklist": {
      "title": "Requisiti di rete del cluster esteso",
      "minBandwidth": "Larghezza di banda min. tra siti",
      "maxLatency": "RTT max tra siti",
      "maxWitnessLatency": "RTT max testimone",
      "jumboFrames": "Jumbo frame (MTU 9000)",
      "required": "Richiesto",
      "witnessBandwidth": "Larghezza di banda min. testimone",
      "highHostNote": "Consultare i TechDocs Broadcom per configurazioni con piu di 15 host/sito."
    }
    ```

    ADD to the "validation" object (after existing "stretchMinHosts" key):

    en.json:
    ```
    "dedicatedMgmtMinHosts": "Dedicated management cluster requires at least 4 hosts (Broadcom KB 392993).",
    "colocatedMinHosts": "Co-located mode: minimum {min} hosts required for this storage type."
    ```

    fr.json:
    ```
    "dedicatedMgmtMinHosts": "Le cluster de gestion dedie necessite au moins 4 hotes (Broadcom KB 392993).",
    "colocatedMinHosts": "Mode co-localise : minimum {min} hotes requis pour ce type de stockage."
    ```

    de.json:
    ```
    "dedicatedMgmtMinHosts": "Dedizierter Management-Cluster erfordert mindestens 4 Hosts (Broadcom KB 392993).",
    "colocatedMinHosts": "Zusammengefasster Modus: Mindestens {min} Hosts fuer diesen Speichertyp erforderlich."
    ```

    it.json:
    ```
    "dedicatedMgmtMinHosts": "Il cluster di gestione dedicato richiede almeno 4 host (Broadcom KB 392993).",
    "colocatedMinHosts": "Modalita co-locata: minimo {min} host richiesti per questo tipo di storage."
    ```

    ADD new top-level "architecture" object inside "deployment" (after "stretchSites"):

    en.json:
    ```
    "architecture": {
      "label": "Management Architecture",
      "shared": "Co-located",
      "dedicated": "Dedicated Domains"
    }
    ```

    fr.json:
    ```
    "architecture": {
      "label": "Architecture de gestion",
      "shared": "Co-localise",
      "dedicated": "Domaines dedies"
    }
    ```

    de.json:
    ```
    "architecture": {
      "label": "Management-Architektur",
      "shared": "Zusammengefasst",
      "dedicated": "Dedizierte Domanen"
    }
    ```

    it.json:
    ```
    "architecture": {
      "label": "Architettura di gestione",
      "shared": "Co-locata",
      "dedicated": "Domini dedicati"
    }
    ```

    CRITICAL: All 4 files must have identical key structure. Only values differ.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/i18n/locales/en.json');const fr=require('./src/i18n/locales/fr.json');const de=require('./src/i18n/locales/de.json');const it=require('./src/i18n/locales/it.json');const keys=['deployment.stretchSites.bandwidthFloorIndicator','deployment.stretchSites.networkChecklist.title','deployment.architecture.label','validation.dedicatedMgmtMinHosts','validation.colocatedMinHosts'];for(const k of keys){const parts=k.split('.');let e=en,f=fr,d=de,i=it;for(const p of parts){e=e[p];f=f[p];d=d[p];i=i[p]}if(!e||!f||!d||!i){console.error('MISSING:',k);process.exit(1)}}console.log('All i18n keys present in all 4 locales')"</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains key `deployment.stretchSites.bandwidthFloorIndicator`
    - en.json contains key `deployment.stretchSites.networkChecklist.title`
    - en.json contains key `deployment.stretchSites.networkChecklist.minBandwidth`
    - en.json contains key `deployment.stretchSites.networkChecklist.maxLatency`
    - en.json contains key `deployment.stretchSites.networkChecklist.maxWitnessLatency`
    - en.json contains key `deployment.stretchSites.networkChecklist.jumboFrames`
    - en.json contains key `deployment.stretchSites.networkChecklist.required`
    - en.json contains key `deployment.stretchSites.networkChecklist.witnessBandwidth`
    - en.json contains key `deployment.stretchSites.networkChecklist.highHostNote`
    - en.json contains key `deployment.architecture.label` with value "Management Architecture"
    - en.json contains key `deployment.architecture.shared` with value "Co-located"
    - en.json contains key `deployment.architecture.dedicated` with value "Dedicated Domains"
    - en.json contains key `validation.dedicatedMgmtMinHosts`
    - en.json contains key `validation.colocatedMinHosts` with `{min}` interpolation parameter
    - fr.json, de.json, it.json each contain ALL the same keys as en.json
    - The verification node command exits 0
  </acceptance_criteria>
  <done>All 4 locale files contain identical key structure for bandwidthFloorIndicator, networkChecklist (7 sub-keys), architecture (3 sub-keys), dedicatedMgmtMinHosts, and colocatedMinHosts. No missing translations.</done>
</task>

<task type="auto">
  <name>Task 2: Update DeploymentModelSelector + create StretchNetworkChecklist + wire ResultsPanel</name>
  <files>src/components/input/DeploymentModelSelector.vue, src/components/results/StretchNetworkChecklist.vue, src/components/results/ResultsPanel.vue</files>
  <read_first>
    - src/components/input/DeploymentModelSelector.vue (current template — understand stretch section structure, existing storeToRefs)
    - src/components/results/ResultsPanel.vue (current component list — understand where to add StretchNetworkChecklist)
    - src/components/shared/WarningBanner.vue (props interface — message:string, severity:'error'|'warning')
    - src/stores/inputStore.ts (after Plan 01 — managementArchitecture ref available)
    - src/stores/calculationStore.ts (after Plan 01 — stretch.bandwidthFloorApplied, stretch.networkChecklist, dedicatedMgmtHostCount, validationErrors available)
    - src/i18n/locales/en.json (after Task 1 — all new keys available)
  </read_first>
  <action>
    STEP 1 — Update src/components/input/DeploymentModelSelector.vue:

    a) Add `managementArchitecture` to storeToRefs destructure from inputStore:
       `const { deploymentMode, preferredSiteHosts, secondarySiteHosts, managementArchitecture } = storeToRefs(input)`

    b) Add `validationErrors` and `dedicatedMgmtHostCount` to storeToRefs from calc store:
       `const { management, stretch, validationErrors, dedicatedMgmtHostCount } = storeToRefs(calc)`

    c) Import WarningBanner:
       `import WarningBanner from '@/components/shared/WarningBanner.vue'`

    d) Add computed for architecture-related validation errors:
       ```
       import { computed } from 'vue'
       const architectureErrors = computed(() =>
         validationErrors.value.filter(e => e.code === 'DEDICATED_MGMT_MIN_HOSTS' || e.code === 'COLLOCATED_MIN_HOSTS')
       )
       ```

    e) Add management architecture toggle INSIDE the stretch template block (after the stretch per-site storage note div, before closing `</template>` of stretch section). Also show in HA mode. Place it as a new section that appears when `deploymentMode !== 'simple'`:
       ```html
       <!-- Management Architecture toggle (ARCH-01/02) — visible in HA and Stretch modes -->
       <template v-if="deploymentMode !== 'simple'">
         <div class="space-y-2 pt-2 border-t border-gray-100 dark:border-gray-700">
           <label class="text-sm font-medium text-gray-700 dark:text-gray-300">
             {{ t('deployment.architecture.label') }}
           </label>
           <div class="flex gap-2">
             <button
               v-for="arch in [
                 { value: 'shared' as const, labelKey: 'deployment.architecture.shared' },
                 { value: 'dedicated' as const, labelKey: 'deployment.architecture.dedicated' },
               ]"
               :key="arch.value"
               :class="[
                 'px-3 py-1.5 text-sm rounded border font-medium transition-colors',
                 managementArchitecture === arch.value
                   ? 'bg-blue-600 text-white border-blue-600'
                   : 'bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 border-gray-300 dark:border-gray-600 hover:border-blue-400'
               ]"
               @click="managementArchitecture = arch.value"
             >
               {{ t(arch.labelKey) }}
             </button>
           </div>
           <!-- Dedicated host count output (ARCH-01) -->
           <div v-if="dedicatedMgmtHostCount !== null" class="text-xs text-gray-500 dark:text-gray-400">
             Management hosts: <span class="font-mono font-semibold">{{ dedicatedMgmtHostCount }}</span>
           </div>
           <!-- Architecture validation errors (ARCH-01/02) -->
           <WarningBanner
             v-for="err in architectureErrors"
             :key="err.code"
             :message="err.code === 'COLLOCATED_MIN_HOSTS' ? t(err.messageKey, { min: err.code === 'COLLOCATED_MIN_HOSTS' ? (input.storageType === 'vsan-esa' ? 3 : 2) : 0 }) : t(err.messageKey)"
             :severity="err.severity"
           />
         </div>
       </template>
       ```
       NOTE: The management architecture template block must be OUTSIDE the `v-if="deploymentMode === 'stretch'"` block — it should be a sibling, guarded by `v-if="deploymentMode !== 'simple'"`.

    f) Update the bandwidth display in the stretch section to show the floor indicator.
       Replace the existing bandwidth div (the blue box with `stretch.minBandwidthGbps.toFixed(2)`) with:
       ```html
       <div class="text-xs text-blue-700 dark:text-blue-400 bg-blue-50 dark:bg-blue-900/30 border border-blue-200 dark:border-blue-700 rounded px-2 py-1">
         {{ t('deployment.stretchSites.bandwidthLabel') }}:
         <span class="font-mono font-semibold">{{ stretch.minBandwidthGbps.toFixed(2) }} Gb/s</span>
         <span v-if="stretch.bandwidthFloorApplied" class="block mt-1 text-amber-600 dark:text-amber-400 text-xs italic">
           {{ t('deployment.stretchSites.bandwidthFloorIndicator') }}
         </span>
       </div>
       ```

    STEP 2 — Create src/components/results/StretchNetworkChecklist.vue:

    Create new file with this content:
    ```vue
    <script setup lang="ts">
    import { useI18n } from 'vue-i18n'
    import { useInputStore } from '@/stores/inputStore'
    import { useCalculationStore } from '@/stores/calculationStore'
    import { storeToRefs } from 'pinia'

    const { t } = useI18n()
    const input = useInputStore()
    const calc = useCalculationStore()
    const { stretch } = storeToRefs(calc)
    </script>

    <template>
      <section
        v-if="input.deploymentMode === 'stretch'"
        class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-4 space-y-3"
      >
        <h3 class="text-sm font-semibold text-gray-900 dark:text-gray-100">
          {{ t('deployment.stretchSites.networkChecklist.title') }}
        </h3>
        <div class="grid grid-cols-2 gap-x-4 gap-y-1 text-xs text-gray-600 dark:text-gray-400">
          <span>{{ t('deployment.stretchSites.networkChecklist.minBandwidth') }}</span>
          <span class="font-mono text-right">{{ stretch.networkChecklist.minInterSiteBandwidthGbps }} Gbps</span>

          <span>{{ t('deployment.stretchSites.networkChecklist.maxLatency') }}</span>
          <span class="font-mono text-right">&lt; {{ stretch.networkChecklist.maxInterSiteLatencyMs }} ms</span>

          <span>{{ t('deployment.stretchSites.networkChecklist.maxWitnessLatency') }}</span>
          <span class="font-mono text-right">&lt; {{ stretch.networkChecklist.maxWitnessLatencyMs }} ms</span>

          <span>{{ t('deployment.stretchSites.networkChecklist.jumboFrames') }}</span>
          <span class="font-mono text-right text-green-600 dark:text-green-400">
            {{ t('deployment.stretchSites.networkChecklist.required') }}
          </span>

          <span>{{ t('deployment.stretchSites.networkChecklist.witnessBandwidth') }}</span>
          <span class="font-mono text-right">{{ stretch.networkChecklist.witnessMinBandwidthMbps }} Mbps</span>
        </div>
        <!-- High host count note (>15 hosts/site) -->
        <p
          v-if="Math.max(input.preferredSiteHosts, input.secondarySiteHosts) > 15"
          class="text-xs text-amber-600 dark:text-amber-400 italic"
        >
          {{ t('deployment.stretchSites.networkChecklist.highHostNote') }}
        </p>
      </section>
    </template>
    ```

    STEP 3 — Update src/components/results/ResultsPanel.vue to include StretchNetworkChecklist:

    a) Add import:
       `import StretchNetworkChecklist from './StretchNetworkChecklist.vue'`

    b) Add `<StretchNetworkChecklist />` in the template, after `<StorageChart />` and before `<ExportToolbar />`:
       ```html
       <StretchNetworkChecklist />
       ```

    STEP 4 — Verify build:
    `npm run build`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/input/DeploymentModelSelector.vue imports WarningBanner
    - src/components/input/DeploymentModelSelector.vue storeToRefs destructures `managementArchitecture`
    - src/components/input/DeploymentModelSelector.vue contains `deployment.architecture.label` i18n key reference
    - src/components/input/DeploymentModelSelector.vue contains `v-if="deploymentMode !== 'simple'"` guard for architecture toggle
    - src/components/input/DeploymentModelSelector.vue contains `stretch.bandwidthFloorApplied` conditional
    - src/components/input/DeploymentModelSelector.vue contains `deployment.stretchSites.bandwidthFloorIndicator` i18n key reference
    - src/components/results/StretchNetworkChecklist.vue exists with `v-if="input.deploymentMode === 'stretch'"` guard
    - src/components/results/StretchNetworkChecklist.vue references `stretch.networkChecklist.minInterSiteBandwidthGbps`
    - src/components/results/StretchNetworkChecklist.vue references `stretch.networkChecklist.maxWitnessLatencyMs`
    - src/components/results/ResultsPanel.vue imports and renders `StretchNetworkChecklist`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>DeploymentModelSelector shows management architecture toggle in HA/Stretch modes with validation errors. Bandwidth display shows floor indicator when applied. StretchNetworkChecklist renders as a card in ResultsPanel when stretch mode is active. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 4 UI changes</name>
  <what-built>
    Phase 4 complete: bandwidth floor indicator, stretch network checklist card, management architecture toggle with validation errors, all in four Swiss locales.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. BANDWIDTH FLOOR (STRCH-06/07):
       - Select "Stretch Cluster" deployment mode
       - Set VMs to 10 and storage per VM to 100 GB (small workload)
       - Verify: bandwidth shows "10.00 Gb/s" (not a sub-10 value)
       - Verify: amber text appears below: "10 Gbps floor applied..."
       - Set VMs to 10000 and storage per VM to 10000 GB (large workload)
       - Verify: bandwidth shows a value > 10 Gb/s
       - Verify: floor indicator text is NOT visible
    3. NETWORK CHECKLIST (STRCH-08):
       - In stretch mode, scroll to the results panel
       - Verify: "Stretch Network Requirements" card is visible
       - Verify card shows: Min inter-site bandwidth = 10 Gbps, Max site-to-site RTT < 5 ms, Jumbo frames = Required
       - Set preferred site hosts to 5, secondary to 5
       - Verify: Max witness RTT shows < 200 ms
       - Set secondary site hosts to 12
       - Verify: Max witness RTT shows < 100 ms
       - Switch to "High Availability" mode
       - Verify: network checklist card DISAPPEARS from results panel
    4. MANAGEMENT ARCHITECTURE (ARCH-01/02):
       - In HA mode, verify: "Management Architecture" toggle visible with "Co-located" / "Dedicated Domains" buttons
       - Click "Dedicated Domains"
       - Set host count to 3
       - Verify: red error banner "...requires at least 4 hosts (Broadcom KB 392993)"
       - Set host count to 4
       - Verify: error disappears
       - Click "Co-located"
       - Set storage to vSAN ESA, host count to 2
       - Verify: yellow warning "Co-located mode: minimum 3 hosts..."
       - Set storage to FC, host count to 1
       - Verify: yellow warning "Co-located mode: minimum 2 hosts..."
       - Switch to "Simple" mode
       - Verify: management architecture toggle NOT visible
    5. LOCALES:
       - Switch language to FR
       - Verify: all new labels (checklist, architecture, validation messages) render in French (no raw key paths visible)
       - Switch to DE, then IT — verify same
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run test` — full suite passes with all Phase 4 tests
2. `npm run build` — production build succeeds
3. Visual verification confirms all 4 requirements (STRCH-06/07, STRCH-08, ARCH-01, ARCH-02) are user-visible
4. All 4 locales render human-readable text for all new UI elements
</verification>

<success_criteria>
- Bandwidth recommendation never falls below 10 Gbps in stretch mode, with visible indicator when floor is applied
- Stretch network checklist card visible only in stretch mode, showing correct RTT thresholds based on host count
- Management architecture toggle visible in HA/Stretch modes with validation enforcement
- All text renders correctly in en, fr, de, it locales
- Zero TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-correctness-and-architecture-validation/04-02-SUMMARY.md`
</output>
