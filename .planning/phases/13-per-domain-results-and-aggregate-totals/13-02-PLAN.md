---
phase: 13-per-domain-results-and-aggregate-totals
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/results/DomainResultCard.vue
  - src/components/results/AggregateTotalsCard.vue
  - src/components/results/ResultsPanel.vue
  - src/components/results/StretchNetworkChecklist.vue
  - src/components/results/VsanMaxClusterCard.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: false
requirements: [RES-01, RES-02, RES-03]

must_haves:
  truths:
    - "Each workload domain displays its own result card showing domain name, recommended host count, CPU utilization %, RAM utilization %, and storage breakdown"
    - "An aggregate totals card shows total host count across all workload domains plus management as the procurement number"
    - "Management domain results render in their existing dedicated section unchanged from v2.x"
    - "Values update reactively when domain inputs change"
  artifacts:
    - path: "src/components/results/DomainResultCard.vue"
      provides: "Per-domain result card accepting DomainResult prop"
      contains: "defineProps"
    - path: "src/components/results/AggregateTotalsCard.vue"
      provides: "Aggregate totals card accepting AggregateTotals prop"
      contains: "defineProps"
    - path: "src/components/results/ResultsPanel.vue"
      provides: "v-for iteration over domainResults"
      contains: "v-for"
  key_links:
    - from: "src/components/results/ResultsPanel.vue"
      to: "src/stores/calculationStore.ts"
      via: "storeToRefs destructure domainResults and aggregateTotals"
      pattern: "storeToRefs.*calc"
    - from: "src/components/results/DomainResultCard.vue"
      to: "src/engine/types.ts"
      via: "DomainResult prop type import"
      pattern: "import type.*DomainResult"
    - from: "src/components/results/AggregateTotalsCard.vue"
      to: "src/engine/types.ts"
      via: "AggregateTotals prop type import"
      pattern: "import type.*AggregateTotals"
---

<objective>
Create per-domain result cards and an aggregate totals card, rewrite ResultsPanel to iterate over domainResults, rewrite StretchNetworkChecklist and VsanMaxClusterCard to accept props, remove broken chart references, and add i18n keys.

Purpose: Deliver RES-01 (per-domain result cards), RES-02 (aggregate totals), and RES-03 (management unchanged) — the core user-facing output of Phase 13.
Output: Working results panel with per-domain cards, aggregate totals card, and passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-per-domain-results-and-aggregate-totals/13-RESEARCH.md
@.planning/phases/13-per-domain-results-and-aggregate-totals/13-01-SUMMARY.md

<interfaces>
<!-- calculationStore exports (the ONLY available computed properties) -->
From src/stores/calculationStore.ts:
```typescript
return { management, domainResults, aggregateTotals, dedicatedMgmtHostCount }
```

From src/engine/types.ts:
```typescript
export interface DomainResult {
  id: string
  name: string
  compute: ComputeResult
  storage: StorageResult
  stretch: StretchResult | null
  vsanMax: VsanMaxResult | null
  validationErrors: ValidationWarning[]
}

export interface AggregateTotals {
  totalRecommendedHosts: number
  totalVmCount: number
  totalRawStorageTB: number
  totalEffectiveStorageTB: number
  allValidationErrors: ValidationWarning[]
}

export interface ComputeResult {
  recommendedHostCount: number
  coreUtilizationPct: number
  ramUtilizationPct: number
  minHostsForCpu: number
  minHostsForRam: number
  totalCoresAvailable: number
  totalCoresRequired: number
  totalRamAvailableGB: number
  totalRamRequiredGB: number
  effectiveHostCount: number
}

export interface StorageResult {
  rawCapacityTB: number
  usableAfterRaidTB: number
  lfsOverheadTB: number
  metadataOverheadTB: number
  effectiveCapacityTB: number
  safeUsableCapacityTB: number
  raidScheme: string
}

export interface StretchResult {
  preferredSiteHosts: number
  secondarySiteHosts: number
  totalHosts: number
  witnessRequired: boolean
  witnessMaxRttMs: number
  stretchMultiplier: number
  stretchMirroringFactor: number
  minBandwidthGbps: number
  checklist: StretchNetworkChecklist
}

export interface VsanMaxResult {
  storageNodeCount: number
  computeNodeCount: number
  rawCapacityTB: number
  usableCapacityTB: number
  raidScheme: string
  profileLabel: string
}
```

From existing HostCountCard.vue (pattern to absorb into DomainResultCard):
```html
<!-- Big number + min-for-cpu + min-for-ram layout -->
<span class="text-5xl font-bold leading-none" :class="...">{{ compute.recommendedHostCount }}</span>
```

From existing i18n keys (en.json results section):
```json
"results": {
  "toolbar": { ... },
  "hostCount": { "title": "...", "recommended": "...", "minForCpu": "...", "minForRam": "..." },
  "vsanMaxTitle": "...", "vsanMaxStorageNodes": "...", "vsanMaxProfile": "...",
  "vsanMaxRawCapacity": "...", "vsanMaxUsableCapacity": "...", "vsanMaxRaidScheme": "...",
  "charts": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DomainResultCard.vue, AggregateTotalsCard.vue, rewrite result components, and update ResultsPanel</name>
  <files>
    src/components/results/DomainResultCard.vue,
    src/components/results/AggregateTotalsCard.vue,
    src/components/results/ResultsPanel.vue,
    src/components/results/StretchNetworkChecklist.vue,
    src/components/results/VsanMaxClusterCard.vue
  </files>
  <read_first>
    - src/components/results/ResultsPanel.vue (current broken template with chart imports)
    - src/components/results/HostCountCard.vue (layout pattern to absorb — big number + min-for-cpu/ram)
    - src/components/results/StretchNetworkChecklist.vue (needs prop rewrite)
    - src/components/results/VsanMaxClusterCard.vue (needs prop rewrite)
    - src/components/results/ExportToolbar.vue (unchanged — still imported in ResultsPanel)
    - src/stores/calculationStore.ts (available exports: management, domainResults, aggregateTotals, dedicatedMgmtHostCount)
    - src/engine/types.ts (DomainResult, AggregateTotals, ComputeResult, StorageResult, StretchResult, VsanMaxResult)
    - src/components/shared/WarningBanner.vue (for validation error display pattern)
    - src/i18n/locales/en.json (existing results.* keys)
  </read_first>
  <action>
**DomainResultCard.vue** (NEW — `src/components/results/DomainResultCard.vue`):
- `defineProps<{ result: DomainResult }>()` — import type from `@/engine/types`
- Use `useI18n()` for `t()` function
- Template layout (absorb HostCountCard pattern):
  - Domain name as card heading: `<h2>{{ result.name }}</h2>`
  - Big number: `result.compute.recommendedHostCount` with emerald/red conditional class (same logic as HostCountCard: `recommendedHostCount <= min hosts` = green, else red)
  - Grid row: "CPU Utilization" label + `result.compute.coreUtilizationPct.toFixed(1)%` with color coding (>80% = text-red-600, else text-emerald-600)
  - Grid row: "RAM Utilization" label + `result.compute.ramUtilizationPct.toFixed(1)%` with same color coding
  - Grid row: "Min for CPU" label + `result.compute.minHostsForCpu`
  - Grid row: "Min for RAM" label + `result.compute.minHostsForRam`
  - Storage section: "Safe Usable Storage" + `result.storage.safeUsableCapacityTB.toFixed(2) TB`
  - Storage row: "RAID Scheme" + `result.storage.raidScheme`
  - Conditional: if `result.stretch !== null`, render `<StretchNetworkChecklist :result="result" />`
  - Conditional: if `result.vsanMax !== null`, render `<VsanMaxClusterCard :result="result" />`
  - Conditional: if `result.validationErrors.length > 0`, render warnings using `t(w.messageKey)` pattern (iterate with v-for)
- Card wrapper: `<div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 break-inside-avoid">`
- Use i18n keys: `results.hostCount.recommended`, `results.hostCount.minForCpu`, `results.hostCount.minForRam`, `results.domain.cpuUtilization`, `results.domain.ramUtilization`, `results.domain.storageUsable`, `results.domain.raidScheme`

**AggregateTotalsCard.vue** (NEW — `src/components/results/AggregateTotalsCard.vue`):
- `defineProps<{ totals: AggregateTotals, managementHostCount: number | null }>()` — import AggregateTotals from `@/engine/types`
- Template:
  - Heading: `t('results.aggregate.title')` — "Total Procurement Summary"
  - Big number: `totals.totalRecommendedHosts` (workload hosts) with label `t('results.aggregate.totalHosts')`
  - If `managementHostCount !== null`: show management hosts row with `managementHostCount`
  - Grand total row: `totals.totalRecommendedHosts + (managementHostCount ?? 0)` with label `t('results.aggregate.grandTotal')` and bold emphasis — this is THE procurement number
  - Row: total VMs `totals.totalVmCount` with label `t('results.aggregate.totalVms')`
  - Row: combined raw storage `totals.totalRawStorageTB.toFixed(2) TB` with label `t('results.aggregate.totalRawStorage')`
  - Row: combined effective storage `totals.totalEffectiveStorageTB.toFixed(2) TB` with label `t('results.aggregate.totalEffectiveStorage')`
  - Conditional: if `totals.allValidationErrors.length > 0`, render all warnings using `t(w.messageKey)`
- Same card wrapper styling as DomainResultCard

**StretchNetworkChecklist.vue** (REWRITE):
- Change from `storeToRefs(calc)` to `defineProps<{ result: DomainResult }>()` — import DomainResult from `@/engine/types`
- Access stretch data via `props.result.stretch` (guaranteed non-null by parent's `v-if`)
- Remove `useCalculationStore()` and `useInputStore()` imports
- Remove `storeToRefs()` calls
- Keep all existing template structure — just change data source from `stretch.value` to `props.result.stretch`
- Keep all existing i18n keys

**VsanMaxClusterCard.vue** (REWRITE):
- Change from `storeToRefs(calc)` + `storeToRefs(input)` to `defineProps<{ result: DomainResult }>()` — import DomainResult from `@/engine/types`
- Access vSAN Max data via `props.result.vsanMax` (guaranteed non-null by parent's `v-if`)
- Remove `useCalculationStore()` and `useInputStore()` imports
- Remove `storeToRefs()` calls
- Replace `storageType === 'vsan-max'` guard with parent-level `v-if` (already handled in DomainResultCard)
- Keep all existing template structure for displaying profile, nodes, capacity
- Keep all existing i18n keys

**ResultsPanel.vue** (REWRITE):
- Remove imports: `HostCountCard`, `CoresChart`, `RamChart`, `StorageChart` (charts deferred to v3.1+ per REQUIREMENTS.md)
- Add imports: `DomainResultCard`, `AggregateTotalsCard`
- Keep imports: `ExportToolbar`, `StretchNetworkChecklist` (now used inside DomainResultCard, but keep import for potential standalone use)
- Actually: `StretchNetworkChecklist` and `VsanMaxClusterCard` are now children of `DomainResultCard`, so remove them from ResultsPanel imports too
- Add store access: `const calc = useCalculationStore()` + `const { domainResults, aggregateTotals, dedicatedMgmtHostCount } = storeToRefs(calc)`
- Template:
  - Print header/footer (keep unchanged)
  - `<DomainResultCard v-for="result in domainResults" :key="result.id" :result="result" />`
  - `<AggregateTotalsCard :totals="aggregateTotals" :management-host-count="dedicatedMgmtHostCount" />`
  - `<ExportToolbar />` (unchanged)
- Remove `computed(() => new Date().toLocaleDateString())` if not used elsewhere (it is used in print header — keep it)

**Delete HostCountCard.vue**: Its display is absorbed into DomainResultCard. Remove the file.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/results/DomainResultCard.vue exists and contains `defineProps<{ result: DomainResult }>`
    - src/components/results/DomainResultCard.vue contains `result.compute.recommendedHostCount`
    - src/components/results/DomainResultCard.vue contains `result.compute.coreUtilizationPct`
    - src/components/results/DomainResultCard.vue contains `result.compute.ramUtilizationPct`
    - src/components/results/DomainResultCard.vue contains `result.storage.safeUsableCapacityTB`
    - src/components/results/AggregateTotalsCard.vue exists and contains `defineProps<{ totals: AggregateTotals`
    - src/components/results/AggregateTotalsCard.vue contains `totals.totalRecommendedHosts`
    - src/components/results/AggregateTotalsCard.vue contains `managementHostCount`
    - src/components/results/ResultsPanel.vue contains `v-for="result in domainResults"`
    - src/components/results/ResultsPanel.vue contains `:key="result.id"`
    - src/components/results/ResultsPanel.vue does NOT contain `HostCountCard` or `CoresChart` or `RamChart` or `StorageChart`
    - src/components/results/StretchNetworkChecklist.vue contains `defineProps<{ result: DomainResult }>`
    - src/components/results/StretchNetworkChecklist.vue does NOT contain `useCalculationStore` or `storeToRefs`
    - src/components/results/VsanMaxClusterCard.vue contains `defineProps<{ result: DomainResult }>`
    - src/components/results/VsanMaxClusterCard.vue does NOT contain `useCalculationStore` or `useInputStore`
    - src/components/results/HostCountCard.vue does NOT exist (deleted)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 5 result components created/rewritten. HostCountCard deleted. Charts removed from ResultsPanel. Build passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys to all 4 locale files</name>
  <files>
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json
  </files>
  <read_first>
    - src/i18n/locales/en.json (existing results.* keys — add new keys alongside, do not remove existing)
    - src/i18n/locales/fr.json (same structure)
    - src/i18n/locales/de.json (same structure)
    - src/i18n/locales/it.json (same structure)
    - src/components/results/DomainResultCard.vue (to see which i18n keys are referenced)
    - src/components/results/AggregateTotalsCard.vue (to see which i18n keys are referenced)
  </read_first>
  <action>
Add these new keys to the `results` section of all 4 locale files. Existing keys under `results.toolbar`, `results.hostCount`, `results.vsanMaxTitle`, `results.charts` MUST be preserved — only ADD new keys.

**en.json** — add inside `"results": { ... }`:
```json
"domain": {
  "cpuUtilization": "CPU Utilization",
  "ramUtilization": "RAM Utilization",
  "storageUsable": "Safe Usable Storage",
  "raidScheme": "RAID Scheme"
},
"aggregate": {
  "title": "Total Procurement Summary",
  "totalHosts": "Workload Hosts",
  "managementHosts": "Management Hosts",
  "grandTotal": "Grand Total (Procurement)",
  "totalVms": "Total VMs",
  "totalRawStorage": "Combined Raw Storage",
  "totalEffectiveStorage": "Combined Effective Storage"
}
```

**fr.json** — French (Switzerland) translations:
```json
"domain": {
  "cpuUtilization": "Utilisation CPU",
  "ramUtilization": "Utilisation RAM",
  "storageUsable": "Stockage utilisable",
  "raidScheme": "Schema RAID"
},
"aggregate": {
  "title": "Resume total des achats",
  "totalHosts": "Hotes de charge",
  "managementHosts": "Hotes de gestion",
  "grandTotal": "Total general (achats)",
  "totalVms": "Total VMs",
  "totalRawStorage": "Stockage brut combine",
  "totalEffectiveStorage": "Stockage effectif combine"
}
```

**de.json** — German (Switzerland) translations:
```json
"domain": {
  "cpuUtilization": "CPU-Auslastung",
  "ramUtilization": "RAM-Auslastung",
  "storageUsable": "Nutzbarer Speicher",
  "raidScheme": "RAID-Schema"
},
"aggregate": {
  "title": "Beschaffungsuebersicht",
  "totalHosts": "Workload-Hosts",
  "managementHosts": "Management-Hosts",
  "grandTotal": "Gesamtsumme (Beschaffung)",
  "totalVms": "Gesamt-VMs",
  "totalRawStorage": "Kombinierter Rohspeicher",
  "totalEffectiveStorage": "Kombinierter Nettokapazitaet"
}
```

**it.json** — Italian (Switzerland) translations:
```json
"domain": {
  "cpuUtilization": "Utilizzo CPU",
  "ramUtilization": "Utilizzo RAM",
  "storageUsable": "Storage utilizzabile",
  "raidScheme": "Schema RAID"
},
"aggregate": {
  "title": "Riepilogo totale acquisti",
  "totalHosts": "Host di carico",
  "managementHosts": "Host di gestione",
  "grandTotal": "Totale generale (acquisti)",
  "totalVms": "Totale VM",
  "totalRawStorage": "Storage grezzo combinato",
  "totalEffectiveStorage": "Storage effettivo combinato"
}
```
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && node -e "const en=require('./src/i18n/locales/en.json'); console.log(en.results.domain.cpuUtilization, en.results.aggregate.grandTotal)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains `"results"` -> `"domain"` -> `"cpuUtilization": "CPU Utilization"`
    - en.json contains `"results"` -> `"aggregate"` -> `"grandTotal": "Grand Total (Procurement)"`
    - fr.json contains `"results"` -> `"domain"` -> `"cpuUtilization": "Utilisation CPU"`
    - de.json contains `"results"` -> `"domain"` -> `"cpuUtilization": "CPU-Auslastung"`
    - it.json contains `"results"` -> `"domain"` -> `"cpuUtilization": "Utilizzo CPU"`
    - All 4 files have both `results.domain` and `results.aggregate` sections
    - Existing `results.toolbar`, `results.hostCount`, `results.charts` keys are preserved in all files
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All i18n keys added to 4 locale files. No existing keys removed. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>src/components/results/ResultsPanel.vue</files>
  <name>Task 3: Visual verification of per-domain results and aggregate totals</name>
  <what-built>
    Per-domain result cards (DomainResultCard.vue) iterating over calculationStore.domainResults, aggregate totals card (AggregateTotalsCard.vue) showing procurement host count, and management domain results (ManagementSummary) unchanged from v2.x.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/vcf-sizer/
    2. With 1 domain: verify result card shows domain name "WLD-1", recommended host count (big number), CPU utilization %, RAM utilization %, safe usable storage, and RAID scheme
    3. Verify aggregate totals card shows "Grand Total (Procurement)" with the sum of workload + management hosts
    4. Add a second domain via "Add Domain" button — verify a second result card appears for "WLD-2"
    5. Change WLD-2 inputs (e.g., increase VM count to 500) — verify WLD-2 card updates reactively while WLD-1 stays unchanged
    6. Verify aggregate totals update to sum both domains
    7. Set a domain to stretch mode — verify stretch network checklist appears inside that domain's card
    8. Set a domain to vSAN Max storage — verify vSAN Max cluster info appears inside that domain's card
    9. Verify ManagementSummary section still shows management overhead (unchanged from v2.x)
    10. Verify charts are NOT shown (deferred to v3.1+)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Human visually verifies per-domain result cards and aggregate totals in the browser. No code changes needed.</action>
  <verify><automated>npm run build</automated></verify>
  <done>User confirms per-domain cards show correct data and aggregate totals display procurement number.</done>
</task>

</tasks>

<verification>
1. `npm run build` — exits 0, zero TypeScript errors
2. `npm run test` — all tests pass (164+ engine + 58 composable tests)
3. Visual: each domain has its own result card in the results panel
4. Visual: aggregate totals card shows procurement host count
5. Visual: management results section unchanged
</verification>

<success_criteria>
- RES-01: Each workload domain renders its own result card with domain name, host count, CPU%, RAM%, storage breakdown
- RES-02: Aggregate totals card shows total host count (workload + management) as procurement number
- RES-03: ManagementSummary renders unchanged from v2.x
- `npm run build` passes
- `npm run test` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/13-per-domain-results-and-aggregate-totals/13-02-SUMMARY.md`
</output>
