---
phase: 23-markdown-preview-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/components/results/ExportToolbar.vue
autonomous: false
requirements:
  - EXPORT-03

must_haves:
  truths:
    - "User clicks a Preview button in the export toolbar and sees a rendered Markdown panel appear"
    - "Preview panel renders tables, headings, and code blocks with correct HTML styling"
    - "Injecting script tags into a domain name does not execute JavaScript in the preview"
    - "User can close the preview panel and return to the results view without data loss"
    - "marked library is loaded lazily on first preview open and does not increase initial page bundle"
  artifacts:
    - path: "src/components/results/ExportToolbar.vue"
      provides: "Preview button alongside existing export buttons, wired to MarkdownPreviewPanel"
      contains: "MarkdownPreviewPanel"
  key_links:
    - from: "src/components/results/ExportToolbar.vue"
      to: "src/components/results/MarkdownPreviewPanel.vue"
      via: "import and v-if toggle"
      pattern: "MarkdownPreviewPanel"
---

<objective>
Wire the MarkdownPreviewPanel into ExportToolbar.vue with a Preview button, then verify the full end-to-end flow including XSS protection and lazy loading.

Purpose: This plan completes EXPORT-03 by connecting the preview component to the user-facing toolbar and validating all 5 success criteria.
Output: Working Preview button in the export toolbar with full-screen Markdown preview panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-markdown-preview-panel/23-RESEARCH.md
@.planning/phases/23-markdown-preview-panel/23-01-SUMMARY.md
@src/components/results/ExportToolbar.vue
@src/components/results/MarkdownPreviewPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MarkdownPreviewPanel into ExportToolbar.vue</name>
  <files>src/components/results/ExportToolbar.vue</files>
  <read_first>
    - src/components/results/ExportToolbar.vue (current full file — buttons, imports, script setup)
    - src/components/results/MarkdownPreviewPanel.vue (props and emits interface)
  </read_first>
  <action>
Modify `src/components/results/ExportToolbar.vue` to add a Preview button and embed the MarkdownPreviewPanel:

1. **Script setup additions:**
   - Add import: `import MarkdownPreviewPanel from '@/components/results/MarkdownPreviewPanel.vue'`
   - Add ref: `const previewOpen = ref(false)`

2. **Template — Add Preview button** BEFORE the Share URL button (first in the toolbar row). Use the same button styling as existing buttons. Disable when `uiStore.localeLoading` is true (same pattern as Markdown export button, prevents PITFALL-10 locale race):
   ```html
   <button
     class="px-3 py-1.5 text-sm font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors disabled:opacity-50"
     :disabled="uiStore.localeLoading"
     @click="previewOpen = true"
   >
     {{ t('results.toolbar.preview') }}
   </button>
   ```

3. **Template — Add MarkdownPreviewPanel component** at the end of the template, AFTER the closing `</div>` of the button row. Place it as a sibling (or inside a wrapping template root):
   ```html
   <MarkdownPreviewPanel :open="previewOpen" @close="previewOpen = false" />
   ```
   If needed, wrap the entire template in a root `<div>` or `<template>` to accommodate the two top-level elements (button row + panel).

4. **Verify** the build passes and the Preview button appears in the toolbar.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && grep "MarkdownPreviewPanel" src/components/results/ExportToolbar.vue && grep "previewOpen" src/components/results/ExportToolbar.vue && grep "results.toolbar.preview" src/components/results/ExportToolbar.vue && grep "localeLoading" src/components/results/ExportToolbar.vue | grep -c "localeLoading" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ExportToolbar.vue imports MarkdownPreviewPanel from `@/components/results/MarkdownPreviewPanel.vue`
    - ExportToolbar.vue contains `const previewOpen = ref(false)`
    - Template contains a button with `@click="previewOpen = true"` and `{{ t('results.toolbar.preview') }}`
    - Preview button has `:disabled="uiStore.localeLoading"` attribute
    - Template contains `<MarkdownPreviewPanel :open="previewOpen" @close="previewOpen = false" />`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Preview button visible in export toolbar, MarkdownPreviewPanel wired with open/close toggle, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Markdown Preview end-to-end</name>
  <files>none</files>
  <action>
Human verification of the complete Markdown preview feature. No code changes — this task validates all 5 ROADMAP success criteria through manual testing in the browser.
  </action>
  <what-built>
    Complete Markdown preview feature: Preview button in export toolbar opens a full-screen modal rendering the sizing report as styled HTML with XSS sanitization and lazy-loaded libraries.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/vcf-sizer/
    2. Configure at least one workload domain with some values, navigate to the results step
    3. In the export toolbar, click the "Preview" button
    4. Verify a full-screen modal appears with the rendered Markdown report:
       - Headings should be styled (larger font sizes)
       - Tables should have borders and proper formatting
       - Code blocks (if any) should be styled
       - The report content should match what "Export Markdown" downloads
    5. Click "Close Preview" — verify you return to the results view with all data intact
    6. Switch locale to French (or another non-English locale), click Preview again — verify headings and labels appear in French
    7. XSS test: Edit a domain name to include `<script>alert('xss')</script>`, then open Preview — verify no JavaScript alert fires and the script tag is stripped from display
    8. Open browser DevTools Network tab, reload the page — verify marked and dompurify chunks are NOT loaded on initial page load. Click Preview — verify they load lazily at that point.
  </how-to-verify>
  <verify>
    <automated>cd /Users/fjacquet/Projects/vcf-sizer && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Preview button visible in export toolbar
    - Clicking Preview opens a modal with styled HTML (not raw Markdown syntax)
    - Tables render with borders, headings render with size differentiation
    - Closing the modal returns to results with all data intact
    - Script tags in domain names are stripped (no JS execution)
    - marked/dompurify chunks load on first Preview click, not on page load
  </acceptance_criteria>
  <done>All 5 Phase 23 success criteria confirmed by human tester</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Markdown -> HTML | Domain names flow through generateMarkdownReport() -> marked.parse() -> v-html; requires sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-01 | Tampering | MarkdownPreviewPanel.vue v-html | mitigate | DOMPurify.sanitize() on all marked.parse() output (PITFALL-9); verified in human checkpoint step 7 |
| T-23-02 | Tampering | Locale race condition | mitigate | Preview button disabled when uiStore.localeLoading is true (PITFALL-10) |
</threat_model>

<verification>
1. `npm run build` passes
2. Preview button appears in toolbar alongside existing buttons
3. Preview opens a modal with styled Markdown content
4. XSS payloads in domain names are sanitized
5. Closing preview preserves all entered data
6. marked/dompurify are lazy-loaded (not in initial bundle)
7. Locale-switched preview shows translated content
</verification>

<success_criteria>
- All 5 ROADMAP success criteria for Phase 23 are met:
  1. Preview button opens rendered Markdown panel (no file download)
  2. Tables, headings, code blocks render with correct HTML styling
  3. Script injection via domain names does not execute
  4. Closing preview returns to results without data loss
  5. marked is lazy-loaded on first preview open
</success_criteria>

<output>
After completion, create `.planning/phases/23-markdown-preview-panel/23-02-SUMMARY.md`
</output>
